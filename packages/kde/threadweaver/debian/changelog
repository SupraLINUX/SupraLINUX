kf6-threadweaver (6.30.0-0supralinux3) resolute; urgency=medium

  * Add the KDE release signing key required by the Debian watch v5 PGP gate.
  * Align Standards-Version with the 4.7.3 policy level recognized by Resolute.
  * Keep absent Multi-Arch metadata semantically equivalent to dpkg-deb's
    reported value "no" in the external package-contract validator.
  * Preserve the 8/8 passing KDE 6.30 autotests from the previous attempt.

 -- SupraLINUX Project <packages@supralinux.invalid>  Sat, 12 Sep 2026 15:45:00 -0300

kf6-threadweaver (6.30.0-0supralinux2) resolute; urgency=medium

  * Remove the SupraLINUX REUSE lint gate from debian/rules: KDE 6.30 does
    not define full-tree REUSE compliance as a package-build requirement,
    and the gate aborted before the real upstream autotests could run.
  * Keep the KDE upstream test suite enabled through dh_auto_test.

 -- SupraLINUX Project <packages@supralinux.invalid>  Sat, 12 Sep 2026 15:10:00 -0300

kf6-threadweaver (6.30.0-0supralinux1) resolute; urgency=medium

  * Package KDE Frameworks threadweaver 6.30.0 from KDE upstream stable.
  * Preserve Debian-family binary package contracts as compatibility inputs.
  * Build against SupraLINUX ECM 6.30.0 and KDE-selected Qt >= 6.9.
  * Keep QDoc outside the default package build until a common Frameworks policy is selected.

 -- SupraLINUX Project <packages@supralinux.invalid>  Sat, 12 Sep 2026 14:35:00 -0300
