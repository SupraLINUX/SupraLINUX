kf6-karchive (6.30.0-0supralinux3) resolute; urgency=medium

  * Record the second clean package attempt as a real packaging-stage FAIL after
    5/5 upstream tests passed.
  * Install the upstream KF6Archive.pc pkg-config file in libkf6archive-dev so
    dh_missing no longer rejects a valid development artifact left in debian/tmp.
  * Keep KDE 6.30 source, dependencies, tests, ABI baseline and binary contracts unchanged.

 -- SupraLINUX Project <packages@supralinux.invalid>  Mon, 14 Sep 2026 07:30:00 -0300

kf6-karchive (6.30.0-0supralinux2) resolute; urgency=medium

  * Record the first clean package attempt as a real configure-stage FAIL.
  * Add qt6-tools-dev because ECM ECMPoQmTools requires Qt6::LinguistTools to
    configure KArchive's translation handling on the Ubuntu Resolute provider.
  * Keep KDE 6.30 source, compression backends, tests and ABI reference unchanged.

 -- SupraLINUX Project <packages@supralinux.invalid>  Sun, 13 Sep 2026 15:50:00 -0300

kf6-karchive (6.30.0-0supralinux1) resolute; urgency=medium

  * Package KDE Frameworks karchive 6.30.0 from KDE upstream stable.
  * Preserve Debian-family binary package names and Multi-Arch contracts as
    compatibility inputs while selecting dependencies from KDE 6.30 requirements.
  * Build against retained SupraLINUX ECM 6.30 and KDE-selected Qt >= 6.9.
  * Keep KDE upstream autotests enabled and keep QDoc outside the default build.
  * Use the retained Debian 6.28 symbols tree as the closest technical ABI baseline.

 -- SupraLINUX Project <packages@supralinux.invalid>  Sat, 12 Sep 2026 18:00:00 -0300
