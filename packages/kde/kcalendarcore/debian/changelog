kf6-kcalendarcore (6.30.0-0supralinux5) resolute; urgency=medium

  * Add a reviewed 18-symbol ABI overlay after the successful -4 build exposed
    KDE 6.29/6.30 public APIs missing from the retained Debian 6.28 baseline.
  * Use upstream introduction baselines: ten symbols at 6.29.0 and eight at
    6.30.0; do not suppress Lintian or alter KDE source.
  * Keep the -4 translation split, Python bindings and full test suite intact.

 -- SupraLINUX Project <packages@supralinux.invalid>  Wed, 16 Sep 2026 14:35:00 -0300

kf6-kcalendarcore (6.30.0-0supralinux4) resolute; urgency=medium

  * Split KDE 6.30 translation catalogs into libkf6calendarcore-data.
  * Make runtime and QML packages consume the exact data package revision.
  * Keep upstream Python bindings/tests and the validated build toolchain enabled.

 -- SupraLINUX Project <packages@supralinux.invalid>  Wed, 16 Sep 2026 13:30:00 -0300

kf6-kcalendarcore (6.30.0-0supralinux3) resolute; urgency=medium

  * Add explicit python3-setuptools build dependency so ECM's
    python3 -m build --wheel --no-isolation can import setuptools.build_meta.
  * Keep the validated Clang/LLVM binding toolchain and KDE 6.30.0 source,
    Python bindings and upstream test defaults unchanged.

 -- SupraLINUX Project <packages@supralinux.invalid>  Wed, 16 Sep 2026 11:10:16 -0300

kf6-kcalendarcore (6.30.0-0supralinux2) resolute; urgency=medium

  * Add explicit clang, libclang-dev and llvm-dev build dependencies so
    Shiboken/ApiExtractor can locate Clang's built-in headers in clean sbuild.
  * Keep KDE 6.30.0 source, Python bindings and upstream test defaults unchanged.

 -- SupraLINUX Project <packages@supralinux.invalid>  Wed, 16 Sep 2026 10:35:00 -0300

kf6-kcalendarcore (6.30.0-0supralinux1) resolute; urgency=medium

  * SupraLINUX KDE Frameworks 6.30.0 Batch 7 clean package preflight.
  * Preserve upstream Python bindings and test defaults; do not inherit
    downstream BUILD_PYTHON_BINDINGS=OFF or BUILD_TESTING=OFF shortcuts.
  * Install Python extension modules using Debian's deb_system sysconfig layout.
  * Retain Debian 6.28 public symbols/copyright only as technical compatibility
    baselines; KDE upstream 6.30.0 remains source and feature authority.

 -- SupraLINUX Project <packages@supralinux.invalid>  Wed, 16 Sep 2026 09:45:00 -0300
