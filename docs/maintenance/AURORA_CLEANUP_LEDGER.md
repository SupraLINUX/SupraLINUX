# Aurora build/infrastructure cleanup ledger

Status: **ACTIVE**

Purpose: keep an explicit record of temporary build infrastructure, caches, diagnostic workflows, host paths, and other engineering artifacts that must be removed when their retention condition expires. This file is intentionally long-lived; entries are removed only after cleanup is executed and verified.

## Rules

- Do not delete an item merely because it is currently unused if it is still part of an open certification or required for reproducibility.
- Every host-side path must record the machine, purpose, provenance, retention condition, deletion command/procedure, and verification step before it is created.
- Temporary CI workflows remain until the investigation/certification evidence they support has been incorporated into durable documentation and no rerun is required.
- Cleanup must not remove canonical certification evidence, source provenance, manifests, or Release assets required to reproduce Aurora.
- Mark an entry `REMOVED` only after cleanup is actually executed and verified.

## Ledger

| Item | Location | Status | Purpose | Remove when | Cleanup / verification |
|---|---|---|---|---|---|
| Legacy self-hosted certified-slice target | `espadarunica:/srv/supralinux/archive/20260829T022000Z/` | **SUPERSEDED — DO NOT CONSUME** | Original machine-local snapshot target. The accepted durable input is now the independently validated GitHub Release asset. Runs `33459607459` attempts 1/2 never promoted this target to canonical state. | After confirming no unrelated local workflow uses it. | Inspect first. If no valid `COMPLETE` tree exists, remove the obsolete path. If one unexpectedly exists, validate/preserve evidence before deletion. |
| Legacy self-hosted snapshot staging | `espadarunica:/srv/supralinux/archive/.staging-20260829T022000Z/` | **OBSOLETE PARTIAL STAGING** | Partial materialization from the abandoned self-hosted acquisition path. It is not certification input. | May be removed after confirming no diagnostic still references it. | `rm -rf /srv/supralinux/archive/.staging-20260829T022000Z`; verify no `.part`/`.bad-*` objects remain in the old staging namespace. |
| Hosted snapshot materialization/publication workflow | `.github/workflows/ksq-snapshot-slice-materialize.yml` | **PUBLICATION COMPLETE — RETAIN FOR PROVENANCE/VERIFICATION** | Produced the deterministic snapshot Release and independently re-downloaded/validated the published bytes. | Retain while snapshot `20260829T022000Z` is Aurora's active certified archive input, or split one-time publication from maintained verification later. | Do not rerun publication against the existing immutable tag unless intentionally converted to verification-only behavior. Preserve publication run, Release identity, SHA-256 and size in validation docs. |
| Hosted snapshot resumable staging caches | GitHub Actions cache keys `aurora-ksq-snapshot-staging-20260829T022000Z-*` | **OBSOLETE / UNTRUSTED ACCELERATION** | Temporary cache used only to survive Snapshot Service transport failures during materialization. Never a trust source. | Eligible for deletion now that the published asset is independently validated and repository-pinned. | Delete matching cache keys; verify no consumer workflow reads them and the Release asset remains independently retrievable. |
| Durable certified snapshot Release asset | Release tag `ksq-snapshot-20260829T022000Z`, asset ID `538944111` | **ACTIVE CANONICAL ENGINEERING INPUT — DO NOT DELETE** | Byte-preserved certified Ubuntu/Debian snapshot slice consumed by KSQ. Exact bytes `973148160`, SHA-256 `8dc9087b53e90c085333644ffdb828b87ce0af6cd61ec5f1d516fe35e80529e7`. | Retain for the full reproducibility/regression/rollback lifetime of Aurora unless migrated byte-for-byte to another documented durable store. | Before any replacement/removal, independently verify replacement identity and rerun the consumer validator. |
| GitHub-hosted Ubuntu 26.04 builder profile probe | `.github/workflows/ksq-github-hosted-builder-profile-probe.yml` | **PASS — RETAIN UNTIL FORMAL BUILDER MIGRATION CLOSES** | Proved hosted Ubuntu 26.04, scoped AppArmor, uidmap normalization, real mmdebstrap and real sbuild. Run `33467690494`, artifact `9785447790`. | After the maintained formal KSQ builder performs the same invariants and the probe's evidence is fully represented in durable docs. | Delete diagnostic workflow only after replacement gate exists; retain run/artifact provenance. |
| Hosted local-snapshot consumer gate | `.github/workflows/ksq-github-hosted-local-snapshot-gate.yml` | **PASS — RETAIN AS REGRESSION GATE** | Proved repository-pinned Release identity, 244 exact candidates, 1541-object closure, local-only APT/toolchain, mmdebstrap, sbuild and AppArmor with network physically absent. Run `33528728431`, artifact `9808961368`. | Keep while the local-slice architecture remains active; later fold into formal builder preflight rather than deleting outright. | Any replacement must preserve fail-closed SHA/size validation, local-only transport, procfs functional proof, command RC capture and AppArmor audit. |
| Source-001 local-slice research probe | `.github/workflows/ksq-source-001-local-slice-probe.yml` | **PASS — RETAIN UNTIL RANGE BUILDER ABSORBS CONTRACT** | Proved certified DAG order 001 (`kf6-extra-cmake-modules 6.29.0-0ubuntu1`) end-to-end from local `deb-src` through prepared Supra version and sbuild. Final run `33531933805`, artifact `9810147299`. Also proved required `Acquire::Source-Symlinks=false` behavior for regular source objects. | After maintained range builder uses the same local source-acquisition contract and source 001 is regressed there. | Delete probe only after range-builder replacement passes; retain final run/artifact and root-cause notes in validation docs. |
| GitHub-hosted parity diagnostic | `.github/workflows/ksq-github-hosted-parity.yml` | **HISTORICAL — RETAIN TEMPORARILY** | A/B measurement of hosted vs self-hosted container/userns behavior. | After formal hosted builder migration and relevant evidence is durable. | Delete workflow; preserve documented conclusions. |
| sbuild live-transport diagnostic | `.github/workflows/ksq-sbuild-live-transport-probe.yml` | **HISTORICAL — RETAIN TEMPORARILY** | Isolated sbuild/AppArmor behavior from Snapshot Service transport failures. | After formal local-slice builder migration. | Delete diagnostic workflow; canonical local-slice regression remains. |
| GitHub-hosted snapshot transport probe | `.github/workflows/ksq-snapshot-transport-hosted-probe.yml` | **HISTORICAL — RETAIN TEMPORARILY** | Historical Snapshot Service transport comparison. | After formal local-slice builder migration. | Delete workflow after measurements/provenance remain documented. |
| KSQ-1 self-hosted representative smoke | `.github/workflows/ksq-1-self-hosted-representative.yml` | **LEGACY / SUPERSEDED** | Historical self-hosted root-cause work. | After formal hosted builder migration has passed required regressions. | Delete workflow only after replacement path is maintained and historical evidence remains documented. |
| `espadarunica` GitHub Actions runner service/config | `espadarunica` / `actions.runner.SupraLINUX-SupraLINUX.espadarunica.service` and runner work/config directory | **FALLBACK / HISTORICAL** | Ubuntu 24.04 host used during root-cause investigation. It is not the selected hosted KSQ execution path. | After formal hosted local-slice builder is operational and no unrelated workflow needs this runner. | Disable/stop/unregister runner only after checking unrelated uses. Do not alter the Ubuntu 24.04 host itself merely for cleanup. |
| Snapshot-slice size measurement workflow | `.github/workflows/ksq-snapshot-slice-size-probe.yml` | **MEASUREMENT COMPLETE — RETAIN TEMPORARILY** | Exact metadata-only measurement: 1541 binary objects + 305 source objects, raw upper bound `1001129661` bytes. Run `33455898813`, artifact `9781553137`. | After formal builder migration and no rerun is required. | Delete workflow; retain durable report/artifact provenance/manifests. |
| Snapshot-slice live archive control | `.github/workflows/ksq-snapshot-slice-size-live-control.yml` | **DIAGNOSTIC COMPLETE — RETAIN TEMPORARILY** | Demonstrated current-live archive drift for certified package versions. Run `33456358421`, artifact `9781682967`. | After formal local-slice builder migration and documentation is stable. | Delete workflow; retain documented drift evidence. |

## Current cleanup decision

The published snapshot Release is canonical engineering input and must not be treated as disposable CI data. Diagnostic workflows may be removed only after their contracts are absorbed into maintained formal workflows and the replacement regressions pass.

The old self-hosted snapshot target/staging and hosted resumable staging caches are no longer part of the active architecture. They may be removed after verifying there is no unrelated dependency; their removal is cleanup only and does not change certified snapshot identity.

## Future entries

Add every non-product host modification or disposable engineering resource here before or at the time it is introduced, including local mirrors/caches, temporary AppArmor profiles if superseded, runner-specific test files, transient repositories, diagnostic workflow files, staging directories, and one-off services.
