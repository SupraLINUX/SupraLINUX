# Aurora build/infrastructure cleanup ledger

Status: **ACTIVE**

Purpose: keep an explicit record of temporary build infrastructure, caches, diagnostic workflows, host paths, and other engineering artifacts that must be removed when their retention condition expires. This file is intentionally long-lived; entries are removed only after cleanup is executed and verified.

## Rules

- Do not delete an item merely because it is currently unused if it is still part of an open certification or required for reproducibility.
- Every host-side path must record the machine, purpose, provenance, retention condition, deletion command/procedure, and verification step before it is created.
- Temporary CI workflows remain until the investigation/certification evidence they support has been incorporated into durable documentation and no rerun is required.
- Cleanup must not remove canonical certification evidence, source provenance, manifests, or release artifacts required to reproduce Aurora.
- Mark an entry `REMOVED` only after verification; do not silently delete the row.

## Ledger

| Item | Location | Status | Purpose | Remove when | Cleanup / verification |
|---|---|---|---|---|---|
| Legacy self-hosted certified-slice target | `espadarunica:/srv/supralinux/archive/20260829T022000Z/` | **SUPERSEDED TARGET — DO NOT CONSUME** | Original machine-local target for snapshot `20260829T022000Z`. The architecture has moved to a durable GitHub Release asset consumed by GitHub-hosted Ubuntu 26.04 jobs. A final certified tree was not promoted by runs `33459607459` attempts 1/2. | After the GitHub-hosted published-slice path passes independent asset verification, local-only APT, mmdebstrap, sbuild smoke and source-001 regression. | Inspect first. If no certified `COMPLETE` tree exists, remove the obsolete path. If a completed tree unexpectedly exists, validate and preserve evidence before deletion. Verify no current workflow/config references this host path. |
| Legacy self-hosted snapshot staging | `espadarunica:/srv/supralinux/archive/.staging-20260829T022000Z/` | **LIKELY PARTIAL — RETAIN UNTIL HOSTED MATERIALIZATION SETTLED** | Resumable staging produced by the abandoned self-hosted materialization path. Run `33459607459` failed during remote Snapshot Service transport before final validation/promotion. This staging is never certification input. | After the durable hosted Release asset is independently validated, or after explicitly deciding the partial staging has no further value for one-time acquisition. | `rm -rf /srv/supralinux/archive/.staging-20260829T022000Z`; verify no final/current workflow depends on it and no `.part`/`.bad-*` objects remain elsewhere. |
| Legacy self-hosted snapshot materialization workflow | `.github/workflows/ksq-snapshot-slice-materialize.yml` | **CREATED — MUST BE REPLACED BEFORE NEXT RUN** | Current file still encodes the former `espadarunica` self-hosted target and `/srv/supralinux/archive` bind. It must not be used as the next canonical materialization path. | After replacement by a GitHub-hosted `ubuntu-26.04` materialization/publication workflow and successful durable publication. | Replace or delete the self-hosted implementation only after preserving run `33459607459` failure evidence in docs. Verify no canonical workflow targets its old self-hosted labels/host path. |
| GitHub-hosted Ubuntu 26.04 builder profile probe | `.github/workflows/ksq-github-hosted-builder-profile-probe.yml` | **PASS — RETAIN TEMPORARILY** | Proved GitHub-hosted `ubuntu-26.04` can load the narrow AppArmor profile, launch the pinned Resolute container, normalize uidmap, pass namespace preflight, complete real mmdebstrap and complete real sbuild. Canonical architecture probe: run `33467690494`, job `99730854792`, artifact `9785447790`, digest `sha256:fbcaa74ec5810f4495396c1f0afe8eb3f1b1ab67ed34723ded0ffed12e293988`. Observed runner image `20260824.116.1`, kernel `7.0.0-1012-azure`. | After the Ubuntu 26.04 hosted local-slice smoke and source-001 regression pass and the architecture is represented by maintained canonical workflows. | Delete diagnostic workflow; retain `docs/validation/AURORA_KSQ_1_GITHUB_HOSTED_RUNNER.md` and artifact provenance. |
| GitHub-hosted parity diagnostic | `.github/workflows/ksq-github-hosted-parity.yml` | **RETAIN TEMPORARILY** | Historical A/B measurement of hosted vs self-hosted container/userns behavior. | After the Ubuntu 26.04 hosted local-slice path is certified and relevant evidence is fully recorded in durable validation docs. | Delete workflow; verify no active validation document depends on rerunning it and no references require the file to exist. |
| sbuild live-transport diagnostic | `.github/workflows/ksq-sbuild-live-transport-probe.yml` | **RETAIN TEMPORARILY** | Isolates `sbuild --chroot-mode=unshare` / AppArmor behavior from Ubuntu Snapshot Service transport failures. Not certification evidence by itself. | After the canonical published-local-slice sbuild smoke passes and the root-cause evidence has been documented. | Delete workflow; verify canonical smoke remains the sole qualification path and docs preserve diagnostic result. |
| GitHub-hosted snapshot transport probe | `.github/workflows/ksq-snapshot-transport-hosted-probe.yml` | **RETAIN TEMPORARILY** | Historical A/B comparison of Snapshot Service transport. | After the published local-slice design is materialized/consumed and measurements are recorded in durable docs. | Delete workflow; verify measurements/provenance are retained in documentation. |
| KSQ-1 self-hosted representative smoke | `.github/workflows/ksq-1-self-hosted-representative.yml` | **LEGACY / SUPERSEDED — RETAIN TEMPORARILY** | Historical representative smoke for self-hosted mmdebstrap/sbuild root-cause work. GitHub-hosted Ubuntu 26.04 run `33467690494` now supersedes it as execution-host qualification. | After the hosted Release-asset local-slice smoke and source-001 regression pass and any acceptance references have been migrated. | Delete only after identifying the replacement canonical workflow and running required regressions. Keep historical run/artifact references in validation docs. |
| `espadarunica` GitHub Actions runner service/config | `espadarunica` / `actions.runner.SupraLINUX-SupraLINUX.espadarunica.service` and runner work/config directory | **RETAIN UNTIL MIGRATION CERTIFIED** | Historical KSQ infrastructure investigation host and temporary rollback/debug path. It is Ubuntu 24.04 and is not the selected canonical execution host. | After GitHub-hosted Ubuntu 26.04 local-slice APT + mmdebstrap + sbuild + source-001 regressions pass and formal KSQ workflows no longer target self-hosted labels. | Disable/stop service, unregister runner from GitHub, remove runner-specific work/config only after checking no unrelated workflow uses it. Do not alter the Ubuntu 24.04 host itself. Verify no `runs-on: self-hosted` KSQ path remains canonical. |
| Future durable certified snapshot Release asset | GitHub Release asset for snapshot `20260829T022000Z` | **PLANNED — NOT CREATED** | Canonical byte-preserved snapshot slice used by GitHub-hosted Ubuntu 26.04 KSQ jobs. Expected raw upper bound `0.9324 GiB`; asset identity will be pinned by SHA-256 and provenance. | Retain for the full reproducibility/regression/rollback lifetime of Aurora's certified snapshot or until migrated byte-for-byte to another documented durable store. | Do not delete as ordinary cleanup while Aurora depends on snapshot `20260829T022000Z`. Before replacement/removal, independently verify replacement asset identity and all provenance/manifests. |
| Snapshot-slice size measurement workflow | `.github/workflows/ksq-snapshot-slice-size-probe.yml` | **MEASUREMENT COMPLETE — RETAIN TEMPORARILY** | Metadata-only exact calculation of the certified snapshot slice. Canonical PASS: run `33455898813`, job `99695669970`, artifact `9781553137`, digest `sha256:39672453ba364d81cfa8621cd060201f845a6d940fb4ac04d45aa25be1ce0e19`. It proved 1541 binary objects + 305 source objects and a conservative raw upper bound of `1001129661` bytes (`0.9324 GiB`; `1.1655 GiB` with 25% headroom), with zero package payloads downloaded. | After final slice manifests/size report are durable, the published slice has been independently validated, and no rerun is required. | Delete workflow; retain the durable report, artifact provenance, manifests and hashes. Verify documentation still records run/artifact/digest and measured totals. |
| Snapshot-slice live archive control | `.github/workflows/ksq-snapshot-slice-size-live-control.yml` | **DIAGNOSTIC COMPLETE — RETAIN TEMPORARILY** | Non-canonical control against the current live archive. Run `33456358421`, job `99697042924`, failed because certified versions of `freerdp3-dev`, `libssl-dev` and `libwinpr3-dev` were no longer available live. Artifact `9781682967`, digest `sha256:2edf9c572a2c134de83216beaae641e66e43cb50a8c5ddf19f6c90a8f1bce419`. This failure is evidence of archive drift, not a KDE/package defect, and cannot replace the certified snapshot measurement. | After this result is recorded in durable snapshot-slice documentation and the published local slice is validated. | Delete workflow; verify the canonical size probe and local-slice provenance remain documented and no acceptance logic references this live control. |

## Future entries

Add every non-product host modification or disposable engineering resource here before or at the time it is introduced, including local mirrors/caches, temporary AppArmor profiles if superseded, runner-specific test files, transient repositories, diagnostic workflow files, staging directories, and one-off services.
