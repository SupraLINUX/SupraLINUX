# SupraLINUX status — 2026-09-16 — Tier 1 Batch 7

Status: **Batch 7 package preparation ready for validation; no Batch 7 package PASS claimed**

Canonical KDE Frameworks Tier 1 remains **18 PASS / 11 pending / 0 current FAIL / 0 BLOCKED**.

## Provider and upstream-layout closure

The `python3-build` provider omission discovered by run `35054417698` is closed. Real provider revalidation passed in run `35087361837`, job `104765243282`, artifact `10442512801`, SHA-256 `49f74d493dd18ed68ecee668f68549cf5f71279fcb96e2f481ceef0eaccf5fa9`.

The corrected upstream-layout probe then passed completely in run `35086226149`, job `104761571679`, artifact `10443202060`, SHA-256 `c50a32b26e18ee84173b5a00cbd85e0c2b380660be9d939eb489c370f1317e77`.

The probe confirms KDE 6.30 defaults `BUILD_PYTHON_BINDINGS=ON` and `BUILD_TESTING=ON` for KCalendarCore, KCoreAddons and KWidgetsAddons, plus `BUILD_DESIGNERPLUGIN=ON` for KWidgetsAddons.

## Batch 7 nodes

Prepared independent nodes:

- KCalendarCore `6.30.0-0supralinux1`
- KCoreAddons `6.30.0-0supralinux1`
- KWidgetsAddons `6.30.0-0supralinux1`

They remain canonical `pending` until real package evidence passes every gate.

KGuiAddons is deferred behind a retained local KCoreAddons PASS because its public KImageCache development surface requires KCoreAddons. This is a package/development-contract dependency even though upstream source-build tiering remains Tier 1.

## Python package contract

Batch 7 exports `DEB_PYTHON_INSTALL_LAYOUT=deb`, installs each extension under `/usr/lib/python3/dist-packages`, and splits them as dedicated `python3-*` packages. The runner requires import smoke for `KCalendarCore`, `KCoreAddons` and `KWidgetsAddons` from the exact locally built package version.

This preserves Debian filesystem conventions without disabling KDE upstream bindings.

## Next gate

Repository Policy and the Batch 7 preparation validator must pass before the real package matrix is allowed to run. The real workflow uses `fail-fast: false` and three independent matrix nodes.

PR #1 remains **OPEN + DRAFT** and must not be merged.
