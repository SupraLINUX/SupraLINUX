# KDE Frameworks Tier 1 Batch 7 — 2026-09-16

Status: **KCalendarCore PASS + KCoreAddons PASS retained; KWidgetsAddons revision -5 prepared**

The canonical Tier 1 manifest still records **18 PASS / 11 pending** until the two retained PASS results are promoted there. This status document deliberately distinguishes that canonical snapshot from newer package evidence.

## KCoreAddons `6.30.0-0supralinux4` — PASS

Run `35122522242`, job `104883541991`:

- artifact `10457958023`
- artifact SHA-256 `c90bb487aec031e71f49a7caaf8483002eb1e07dacf1642bcf1c9fd811473e64`
- rootfs SHA-256 `dd7762ba1cdc25dd57b05e8203b3d5d4d448d9ee7131ac0b99d1d85989854794`
- tests: **34/34 PASS**
- Lintian error gate: PASS
- Python import: PASS
- APT runtime closure: PASS
- C++ consumer smoke: PASS
- SONAME: `libKF6CoreAddons.so.6`
- downstream eligible: yes

The reviewed KDE 6.29 ABI overlay closed this lane without disabling functionality or suppressing Lintian.

## KCalendarCore `6.30.0-0supralinux5` — PASS

Run `35130213945`, job `104909057699`:

- artifact `10461386548`
- artifact SHA-256 `6f191cad05620093d9e97e1df3fec78d82e9f9df74270ba8a6bc6b328c61e2f6`
- rootfs SHA-256 `eb3e6bfe65b142c1308d8a1892e00c6bd57028126147ac09376778dec60692f6`
- tests: **507/507 PASS**
- Lintian error gate: PASS
- Python import: PASS
- APT runtime closure: PASS
- C++ consumer smoke: PASS
- SONAME: `libKF6CalendarCore.so.6`
- downstream eligible: yes

Revision `-4` had already proved that the translation split was correct and that all 507 tests passed. Its only remaining failure was Lintian rejecting 18 new public symbols whose generated minima contained the Debian revision. Revision `-5` classified those APIs by actual upstream introduction release: 10 at KDE 6.29.0 and 8 at KDE 6.30.0. The strict Lintian gate then passed; no suppression or source patch was used.

## KWidgetsAddons history

### Revision `-3`

Run `35108531794` reached the unchanged upstream tests under bare Xvfb. **25/27** suites passed. The only failures were `ktwofingertaptest` and `ktwofingerswipetest`, both at `qWaitForWindowActive()` because no real window manager was present.

### Revision `-4`

Run `35122522242`, job `104883542335`, artifact `10457839499`, artifact SHA-256 `683d8f73a55df01cbef18ac57f231d9c7383c5f2f10df2e3b3e3a327356d75bf`, rootfs SHA-256 `21f3ac34931be06d6c31290f94550e150cc09203050f1901c9fd85a8e01550d1`.

Adding verified Openbox to the entire Xvfb session fixed both activation-dependent gesture suites. The result improved to **26/27 PASS**, but a previously green test then failed: `KSqueezedTextLabelAutotest::testElisionOnResize(normal text)` found the tooltip already populated before its explicit resize.

Upstream `KSqueezedTextLabel` populates the tooltip whenever its measured text width exceeds the current contents rect. The test creates and shows a top-level widget before checking the initial state. Taken together with the `-3`/`-4` A/B evidence, the remaining failure is fixture-induced top-level geometry sensitivity rather than justification to alter KDE source or drop a test.

## KWidgetsAddons revision `-5`

Revision `6.30.0-0supralinux5` partitions the fixture while preserving the complete upstream test inventory:

1. obtain the CTest inventory and require exactly **27** tests;
2. require the non-gesture partition to contain exactly **25** tests;
3. require the Openbox partition to contain exactly **2** tests: `ktwofingertaptest` and `ktwofingerswipetest`;
4. run the 25 non-gesture suites serially under bare Xvfb;
5. start Openbox on that Xvfb display and verify `_NET_SUPPORTING_WM_CHECK`;
6. run exactly the two gesture suites serially under the real window manager.

No upstream test is disabled, deleted, marked expected-failure or ignored. KDE 6.30 source, Python bindings and Designer plugin remain unchanged. A package PASS is not claimed until the real `-5` build completes all package/Lintian/Python/APT/consumer gates.

## CI policy and global discovery

The schema-2 Batch 7 semantic selector ignores result/ledger-only changes and rebuilds only when a node's actual package contract changes. Therefore registering KCalendarCore/KCoreAddons PASS does not spend another `sbuild`; the `-5` KWidgets package-tree delta is the only Batch 7 build expected from this remediation.

Separately, the non-promoting global source campaign run `35131119792` completed with five `DIAG_PASS` and two `DIAG_FAIL`. Those diagnostic results do not alter package PASS/FAIL/BLOCKED states and are documented in `docs/decisions/kde-tier1-source-diagnostic-2026-09-16.md`.

KCoreAddons and KCalendarCore now have retained local package PASS artifacts. KWidgetsAddons remains an independent remediation and does not block unrelated Tier 1 work.
