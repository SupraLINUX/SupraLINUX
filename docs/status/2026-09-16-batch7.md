# KDE Frameworks Tier 1 Batch 7 — 2026-09-16

Status: **KCalendarCore PASS + KCoreAddons PASS retained; KWidgetsAddons revision -6 prepared after real -5 FAIL**

The canonical Tier 1 manifest still records **18 PASS / 11 pending** until the two retained PASS results are promoted there. This document distinguishes that canonical snapshot from newer package evidence.

## KCoreAddons `6.30.0-0supralinux4` — PASS

Run `35122522242`, job `104883541991`, commit `ea465e7f78ee621f6456246e7ba76e0a89e46d78`:

- artifact `10457958023`, SHA-256 `c90bb487aec031e71f49a7caaf8483002eb1e07dacf1642bcf1c9fd811473e64`;
- rootfs SHA-256 `dd7762ba1cdc25dd57b05e8203b3d5d4d448d9ee7131ac0b99d1d85989854794`;
- 34/34 tests PASS;
- Lintian, Python import, APT runtime closure and C++ consumer smoke PASS;
- SONAME `libKF6CoreAddons.so.6`;
- downstream eligible.

The reviewed KDE 6.29 ABI overlay closed this lane without disabling functionality or suppressing Lintian.

## KCalendarCore `6.30.0-0supralinux5` — PASS

Run `35130213945`, job `104909057699`, commit `9b9e313176d5154656274976872feb7be34ab126`:

- artifact `10461386548`, SHA-256 `6f191cad05620093d9e97e1df3fec78d82e9f9df74270ba8a6bc6b328c61e2f6`;
- rootfs SHA-256 `eb3e6bfe65b142c1308d8a1892e00c6bd57028126147ac09376778dec60692f6`;
- 507/507 tests PASS;
- Lintian, Python import, APT runtime closure and C++ consumer smoke PASS;
- SONAME `libKF6CalendarCore.so.6`;
- downstream eligible.

The `-5` ABI overlay classifies 10 public APIs at KDE 6.29.0 and 8 at KDE 6.30.0. No Lintian suppression or KDE source patch is used.

## KWidgetsAddons attempt history

### Revision `-3`

Run `35108531794` under bare Xvfb passed 25/27 suites. Only `ktwofingertaptest` and `ktwofingerswipetest` failed at window activation because no window manager was present.

### Revision `-4`

Run `35122522242`, job `104883542335`, artifact `10457839499`, SHA-256 `683d8f73a55df01cbef18ac57f231d9c7383c5f2f10df2e3b3e3a327356d75bf`.

Running the entire suite under verified Openbox fixed both gesture tests but changed the fixture enough for `KSqueezedTextLabelAutotest::testElisionOnResize(normal text)` to fail. Result: 26/27.

### Revision `-5` — FAIL

Run `35134670336`, job `104923940192`, commit `e0228ddb21fb57a639245b2fb93494854bccd928`:

- artifact `10462188593`, SHA-256 `f5c47f8efbf1ca2172e0578b104e472875f01966b17f33f34eeadf47daef8aa5`;
- rootfs SHA-256 `82eb0b91799d774ab0354ec2efb2a346a4557a1a6884bec860a93cbc678b7042`;
- the CTest inventory assertion proved exactly 27 = 25 non-gesture + 2 gesture suites;
- the bare-Xvfb partition completed 24/25 PASS;
- `KSqueezedTextLabelAutotest` was the only failing suite;
- the two gesture suites were not reached because the first CTest returned failure;
- Openbox had not been started when `KSqueezedTextLabel` failed.

This disproves the hypothesis that the running window manager itself is the direct cause. The upstream test uses default `QFontMetrics` and is sensitive to the fixture's font metrics. The earlier `-3` chroot where it passed contained DejaVu fonts; the later Openbox-derived test environment did not.

## KWidgetsAddons revision `-6`

The remediation makes the test font fixture explicit rather than changing KDE:

1. add `fontconfig` and `fonts-dejavu-core` as `<!nocheck>` build dependencies;
2. require generic `sans-serif` to resolve to a family containing `DejaVu Sans` before CTest;
3. require the unchanged 27 = 25 + 2 CTest inventory;
4. run all 25 non-gesture suites serially under bare Xvfb;
5. start Openbox and verify `_NET_SUPPORTING_WM_CHECK`;
6. run exactly the two gesture suites serially under Openbox.

KDE upstream does not mandate DejaVu; this is a SupraLINUX CI-fixture choice made explicit because the geometry-sensitive test requires deterministic metrics. No upstream test is disabled, expected-failed or patched.

## CI policy

The schema-2 Batch 7 selector ignores result/ledger/documentation-only changes and rebuilds only a node whose build contract changes. Therefore this remediation should execute only KWidgetsAddons; KCalendarCore and KCoreAddons must remain scope-skipped.

The two retained PASS artifacts are ready for canonical promotion with exact per-file hashes extracted from their retained artifacts. That promotion is a metadata/documentation operation and must not rebuild either package.
