# KDE Frameworks Tier 1 Batch 7 — 2026-09-16

Status: **revision -4 prepared; fourth real attempt pending**

Promoted Tier 1 remains **18 PASS / 11 pending / 0 current promoted FAIL / 0 BLOCKED**. The immutable Batch 7 history separately retains nine real FAIL attempts: three nodes across revisions `-1`, `-2` and `-3`.

## What revision -3 proved

Run `35108531794` attempted all three nodes independently with `fail-fast: false`. The earlier Clang/LLVM and Setuptools provider corrections worked: all three builds progressed past Python binding generation and wheel construction.

- KCalendarCore: **507/507 tests PASS**, then `dh_missing` found 18 `kcalendarcore6_qt.qm` catalogs with no binary package owner. Artifact `10450974793`, SHA-256 `50b285f705674945d9edcf0e52553b3087a9ff73089f12ea8f1c761cc66f9514`.
- KCoreAddons: **34/34 tests PASS**; package generation completed, but Lintian rejected two new KAboutData symbols because their generated minimum version included the Debian revision. Artifact `10451234111`, SHA-256 `2ef983e4f1c481100e526ca08234fb11aaece859020c64471658790b190a66c5`.
- KWidgetsAddons: **25/27 tests PASS**; only the two gesture tests requiring `qWaitForWindowActive()` failed under bare Xvfb. Artifact `10451113719`, SHA-256 `9474a630af3074f94f9bfbf9cfca52ebbabd726eafc62e1e61c0ea202fef837e`.

None is `BLOCKED`.

## Revision -4 remediations

### KCalendarCore

Add `libkf6calendarcore-data` and assign the KDE 6.30 translation catalogs to it. Runtime and QML packages depend on the exact data-package source version. Debian 6.30 uses the same technical package split; KDE upstream remains source/feature authority.

### KCoreAddons

Keep the retained Debian 6.28 symbols file as baseline and overlay exactly the two symbols documented upstream as introduced in 6.29:

- `KAboutData::setUrl(...)` -> `6.29.0`
- `KAboutData::url(...) const` -> `6.29.0`

The overlay is applied before the normal `dh_makeshlibs` sequence. Lintian is not overridden or suppressed.

### KWidgetsAddons

Do not disable either failing test. Add Openbox and `x11-utils` only for `<!nocheck>` builds, start Openbox inside Xvfb, verify a real EWMH `_NET_SUPPORTING_WM_CHECK` window id, then run the unchanged test suite serially.

## Global campaign interaction

Batch 7 remains runnable, but it no longer serializes unrelated Tier 1 discovery. The global discovery policy allows independent lanes to be implemented/attempted while these three nodes are remediated.
