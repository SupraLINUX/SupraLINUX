# KDE Frameworks Tier 1 Batch 7 — 2026-09-16

Status: **KCalendarCore PASS + KCoreAddons PASS retained; KWidgetsAddons `-7` prepared after real `-6` Lintian FAIL**

The canonical Tier 1 manifest still records **18 PASS / 11 pending / 0 current FAIL / 0 BLOCKED** until retained PASS evidence is promoted. This status document distinguishes that canonical snapshot from newer package evidence.

## KCoreAddons `6.30.0-0supralinux4` — PASS

Run `35122522242`, job `104883541991`, commit `ea465e7f78ee621f6456246e7ba76e0a89e46d78`:

- artifact `10457958023`, SHA-256 `c90bb487aec031e71f49a7caaf8483002eb1e07dacf1642bcf1c9fd811473e64`;
- rootfs SHA-256 `dd7762ba1cdc25dd57b05e8203b3d5d4d448d9ee7131ac0b99d1d85989854794`;
- **34/34 tests PASS**;
- Lintian, Python import, APT runtime closure and C++ consumer smoke PASS;
- SONAME `libKF6CoreAddons.so.6`;
- downstream eligible.

## KCalendarCore `6.30.0-0supralinux5` — PASS

Run `35130213945`, job `104909057699`, commit `9b9e313176d5154656274976872feb7be34ab126`:

- artifact `10461386548`, SHA-256 `6f191cad05620093d9e97e1df3fec78d82e9f9df74270ba8a6bc6b328c61e2f6`;
- rootfs SHA-256 `eb3e6bfe65b142c1308d8a1892e00c6bd57028126147ac09376778dec60692f6`;
- **507/507 tests PASS**;
- Lintian, Python import, APT runtime closure and C++ consumer smoke PASS;
- SONAME `libKF6CalendarCore.so.6`;
- downstream eligible.

## KWidgetsAddons revisions `-3` through `-5`

`-3` isolated the two activation-dependent gesture failures under bare Xvfb. `-4` proved Openbox fixes both gestures but a global WM fixture changes `KSqueezedTextLabel` geometry. `-5` split the suite into 25 non-gesture + 2 gesture tests and proved the split itself is correct; its only remaining failure was the geometry-sensitive label test under a nondeterministic font environment.

## KWidgetsAddons `6.30.0-0supralinux6` — FAIL after 27/27 tests PASS

Run `35138333353`, job `104936243365`, commit `6b3a8b9f408c5fff4bceec81ac9ffb3e47a4dbd3`:

- artifact `10464950275`, SHA-256 `6ec80fbeeeecb7f119bea3bc2aa2ddaee665b27f52d636119260abccdcbfd883`;
- rootfs SHA-256 `08c582c7f007c64c03c476739a554527b6d8edda4905c8515f0f3224e7810ceb`;
- `sans-serif=DejaVu Sans` assertion PASS;
- 25/25 non-gesture tests PASS under bare Xvfb;
- 2/2 gesture tests PASS under verified Openbox;
- **27/27 upstream suites PASS**;
- binary build completed;
- final result **FAIL** because the strict Lintian gate failed.

The test remediation is therefore validated. The remaining work is packaging, not KDE source or test-fixture behavior.

Lintian found:

1. `libkf6widgetsaddons-dev` packages the Qt Designer plugin `kwidgetsaddons6widgets.so`; `dh_shlibdeps` generated `${shlibs:Depends}` but the package stanza did not consume it, so Lintian reported a missing libc dependency.
2. Five KDE symbols plus `_ZSt19piecewise_construct@Base` were assigned the current Debian revision as minimum.

Upstream 6.28/6.29 comparison establishes that the five KDE APIs appear by **6.29.0** and were absent in 6.28.0. `KAssistantDialog::onLastPage()` is explicitly documented upstream as `since 6.29`. `_ZSt19piecewise_construct` is handled as a toolchain implementation export, consistent with the previously validated BluezQt policy.

## KWidgetsAddons revision `-7`

The candidate remediation:

- keeps KDE 6.30.0 source unchanged;
- keeps Python bindings, Designer plugin and all 27 upstream tests unchanged;
- retains the validated deterministic DejaVu/Xvfb/Openbox fixture;
- adds `${shlibs:Depends}` to `libkf6widgetsaddons-dev`;
- applies a six-line ABI overlay before `dh_makeshlibs`:
  - five KDE APIs at minimum `6.29.0`;
  - `_ZSt19piecewise_construct@Base` as `(optional=toolchain)` at minimum `6.30.0`.

No Lintian suppression is introduced. A PASS will be recorded only if the real build reaches every package/runtime/consumer gate.

## Global source diagnostic status

The third non-promoting source campaign, run `35138333645` on commit `6b3a8b9f408c5fff4bceec81ac9ffb3e47a4dbd3`, completed **7 DIAG_PASS / 0 DIAG_FAIL**. KConfig and KI18n remediations are both validated. Diagnostic PASS does not promote package state.

## Next dependency lane

KGuiAddons remains deferred until Batch 7 state is cleanly recorded. Its public `KImageCache` header includes the KCoreAddons `KSharedDataCache` surface, so its development/consumer validation must consume the retained local KCoreAddons PASS rather than an Ubuntu KDE package.

PR #1 remains **OPEN + DRAFT**. No merge is authorized.
