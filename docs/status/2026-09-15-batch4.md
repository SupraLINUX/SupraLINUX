# SupraLINUX status — 2026-09-15 — Batch 4 remediation

## Canonical starting point

PR #1 remains **OPEN + DRAFT**. The branch started Batch 4 from canonical Tier 1 state:

**10 PASS / 19 pending / 0 current FAIL / 0 BLOCKED**.

No authoritative KVM/JIT PASS is claimed here; this is still the hosted non-authoritative package preflight lane.

KDE Frameworks `6.30.0` remains the selected stable upstream release. KDE upstream is authority; Ubuntu Resolute remains provider/compatibility target.

## Real Batch 4 attempt

Workflow run `34999449194` from commit `209f6234cbd94d0b4e02df509b25ae5a6e0922fd` attempted all three selected independent nodes.

### KItemViews

KItemViews `6.30.0-0supralinux1` is a real hosted-preflight PASS:

- job `104483912528`;
- artifact `10409267184`;
- artifact SHA-256 `7e34fa7ede21510cf6829448e7b45a5c2832aaf9ac2719b9cb4125d23b593e55`;
- tests `2/2 PASS`;
- Lintian error gate PASS;
- SONAME `libKF6ItemViews.so.6` PASS;
- consumer smoke PASS.

Its evidence is retained. Canonical promotion remains deferred until Batch 4 closure, so the global Tier 1 snapshot is not changed by this intermediate state.

### KGlobalAccel

KGlobalAccel `6.30.0-0supralinux1` is a historical real FAIL:

- job `104483912661`;
- artifact `10408264332`;
- artifact SHA-256 `1b7564f211967d1be8b61a96bc2a67389c6164f566e54dad79682d37a33bf6e5`;
- failure stage `sbuild` / `dh_auto_configure` / CMake;
- tests not reached.

Root cause: ECM `ECMPoQmTools` requires Qt 6 `LinguistTools`, but `qt6-tools-dev` was absent from Build-Depends.

Remediation candidate: `6.30.0-0supralinux2`, adding `qt6-tools-dev (>= 6.9.0~)`. No KDE source change is made.

### KSyntaxHighlighting

KSyntaxHighlighting `6.30.0-0supralinux1` is also a historical real FAIL:

- job `104483912313`;
- artifact `10408154532`;
- artifact SHA-256 `8601077898fcba1ea5708fbd4e5a1c0d877825f969b8b39999d7d6bf131fbfe4`;
- failure stage `sbuild` / `dh_auto_configure` / CMake;
- tests not reached.

It has the same independently observed root cause: ECM could not resolve required Qt 6 `LinguistTools` because `qt6-tools-dev` was missing from Build-Depends.

Remediation candidate: `6.30.0-0supralinux2`, adding `qt6-tools-dev (>= 6.9.0~)`. No KDE source change is made.

## Provider verification

Ubuntu Resolute provides `qt6-tools-dev` `6.10.2-1` and that package contains the `Qt6LinguistTools` CMake configuration required by ECM. The repair therefore uses the existing Ubuntu Qt 6.10.2 provider; it does not alter KDE authority or the selected Qt series.

## Repository Policy incident

Repository Policy for preparation commit `209f6234cbd94d0b4e02df509b25ae5a6e0922fd` passed all earlier validators and failed only in the Batch 4 validator with `KeyError: 'kitemviews'`.

That exception was a validator-state assumption, not a package failure. The in-flight Batch 4 truth belongs in the campaign ledger while the canonical Tier 1 manifest remains pending until closure. The corrected validator therefore checks the real campaign evidence and current `pending` canonical state without requiring a premature DAG PASS/promotion.

No existing package gate is relaxed.

## Remediation state

The Batch 4 ledger now records:

- KItemViews: real PASS retained;
- KGlobalAccel `-1`: real FAIL retained; `-2` remediation pending build;
- KSyntaxHighlighting `-1`: real FAIL retained; `-2` remediation pending build.

Canonical Tier 1 remains:

**10 PASS / 19 pending / 0 current FAIL / 0 BLOCKED**.

This distinction is intentional. A real attempt result is evidence; canonical downstream eligibility is a separate batch-closure transition.

The scope expectation for the remediation commit is:

- KItemViews scope-skip;
- KGlobalAccel `-2` real rebuild;
- KSyntaxHighlighting `-2` real rebuild.

PR #1 remains Draft and must not be merged.
