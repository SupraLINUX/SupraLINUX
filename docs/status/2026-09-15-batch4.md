# SupraLINUX status — 2026-09-15 — Batch 4 preparation

## Starting point

PR #1 remains **OPEN + DRAFT**. Batch 3 is closed and Repository Policy is green at the starting HEAD `f25f62b7ac24c156a782ab5dae3b7619e02e164a`.

Canonical KDE Frameworks Tier 1 state before Batch 4 attempts is:

**10 PASS / 19 pending / 0 current FAIL / 0 BLOCKED**.

No authoritative KVM/JIT PASS is claimed by this work; the hosted package lane remains non-authoritative preflight.

## Upstream authority

KDE Frameworks 6.30.0 remains the selected stable upstream Frameworks release. No version transition is made for Batch 4.

Batch 4 selection follows KDE 6.30 build metadata first and uses Ubuntu Resolute only as provider/compatibility target. Debian/Ubuntu package metadata remains reference material.

## Batch 4 selection

Selected nodes:

- KItemViews (`kitemviews`);
- KGlobalAccel (`kglobalaccel`);
- KSyntaxHighlighting (`syntax-highlighting`).

All three are Tier 1 nodes whose KDE predecessor set is only the retained ECM PASS. They remain independent and can be attempted in parallel.

KConfig stays deferred pending explicit multi-library ABI/symbol support in the package runner. KWidgetsAddons is also deferred from this batch because the KDE Linux/shared default enables Python bindings, which adds Shiboken6/PySide6 complexity while simpler independent nodes remain available.

## Packaging preparation

All three begin at `6.30.0-0supralinux1`. The package metadata:

- preserves Debian-family binary package names and relevant Architecture/Multi-Arch contracts for Ubuntu compatibility;
- keeps QCH disabled and uses truthful `-doc` compatibility stubs;
- enables upstream tests;
- consumes retained ECM `6.30.0-0supralinux3`;
- injects verified Debian symbols/copyright references from run `34708030450`, artifact `10301938362`;
- validates exact upstream source SHA-256;
- retains artifacts, Lintian evidence, SONAME checks and consumer smoke evidence.

The new Batch 4 dependency contract checker also validates selected per-binary Depends/Recommends relationships instead of checking only the development package's runtime/Qt baseline.

## Dependency review corrections

Reference packaging was not copied blindly:

- inherited `libxkbcommon-dev` entries are omitted where the selected KDE 6.30 build path does not require them;
- KItemViews keeps its upstream-default Designer plugin and therefore uses `qt6-tools-dev`;
- KGlobalAccel uses `qt6-base-private-dev` because KDE 6.30 requires `Qt6GuiPrivate` with Qt >= 6.10, and `qt6-declarative-dev` because the enabled upstream tests require QtQml;
- KSyntaxHighlighting keeps required Perl, builds the available Qt Quick/QML path, and deliberately enables optional XercesC compile-time syntax validation without turning optional Python into a hard dependency.

The stale dependency document claims of `28 pending` are corrected to the real canonical **10 PASS / 19 pending** state.

## State boundary

This is preparation only. KItemViews, KGlobalAccel and KSyntaxHighlighting remain `pending`, are not downstream-eligible and have no fabricated PASS evidence.

A real node attempt may become PASS or FAIL. BLOCKED is not applicable unless a predecessor fails; the retained ECM predecessor is already PASS. PR #1 remains Draft and must not be merged as part of this preparation.
