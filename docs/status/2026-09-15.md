# SupraLINUX status — 2026-09-15

## KDE Frameworks Tier 1 Batch 3

Batch 3 is technically and canonically closed as PASS for `kitemmodels`, `bluez-qt` and `kplotting`.

### Retained PASS evidence

- KItemModels `6.30.0-0supralinux1`: PASS from workflow run `34896417969`, job `104151531993`, 13/13 tests, artifact `10369501432`, artifact SHA-256 `b806eaf27f733c1a2b5cc1d108ca442fef673b6dc0a53cfc5fff38abd2bf6732`.
- KPlotting `6.30.0-0supralinux1`: PASS from workflow run `34896417969`, job `104151532320`, 5/5 tests, artifact `10369086459`, artifact SHA-256 `f4c4f7425e582b5001fdffced34d045dc46152074422bd2bcf994e544bb96bb8`.

### BluezQt remediation PASS

BluezQt attempt 1 remains a historical real FAIL: package `6.30.0-0supralinux1`, workflow run `34896417969`, job `104151532330`, artifact `10369725265`, SHA-256 `ab85b1c2eeef6bf2dc9d622d6695b95427cca6e246ac13a0f825034e9e63d195`, after 18/18 upstream tests. The failure was the Lintian symbols contract for `_ZSt19piecewise_construct@Base`; it is not reclassified as BLOCKED.

BluezQt `6.30.0-0supralinux2` then passed a clean remediation build from commit `b58b5e6072bb14487e304857d34a53659ec50f60`:

- workflow run `34945979836`;
- job `104305337324`;
- artifact `10387429776`;
- artifact SHA-256 `db8a3718a31eaae00d5f9fbdb60014dfeb1faf1c2bc84a88f9ab0d9bffec07ed`;
- upstream tests `18/18 PASS`;
- Lintian error gate PASS;
- SONAME and consumer smoke PASS;
- downstream eligible.

The remediation preserves the reviewed deterministic symbols transform and its pinned hashes. No upstream source or KDE dependency change was required.

### Repository Policy validator

Repository Policy run `34945979830`, job `104305336760`, failed because the Batch 3 validator searched for the first textual occurrence of `dh_makeshlibs` and incorrectly treated the Make target `override_dh_makeshlibs:` as command execution. The real `debian/rules` recipe already executes the symbols transform before `dh_makeshlibs`.

The validator now identifies actual recipe command lines and explicitly distinguishes `override_dh_makeshlibs:` from a real `dh_makeshlibs` invocation. This is a validator fix; BluezQt packaging is not changed to accommodate a false positive.

### Batch 3 closure documentation incident

Closure commit `190a4b7b42045cb839dbab6387f1f132a42ae8aa` triggered Repository Policy run `34980179606`, job `104418151903`. Repository, Qt and shell/KVM policy checks passed, but `validate_kde_dag.py` failed because the rewritten `docs/kde-dag.md` had condensed away mandatory historical ECM failure evidence, the exact BLOCKED semantic statement, the explicit separate upstream-test gate, and the second ECM remediation details (`lintian --fail-on error` plus `qtpaths6`).

This is a documentation/policy regression only. It does not change any Framework package state, does not invalidate the three Batch 3 PASS artifacts, and must not be recorded as a package FAIL. The DAG documentation is restored from the prior complete history and extended with Batch 3 rather than replacing historical evidence.

### Tier 1 canonical-state validator incident

Repository Policy run `34980566171`, job `104419480869`, passed the restored KDE DAG validator and then failed at `validate_kde_tier1.py`. That validator still encoded the pre-Batch-3 canonical set: seven explicit PASS nodes and `7 PASS / 22 pending`. It therefore misclassified `bluez-qt`, `kitemmodels` and `kplotting` as nodes that should still be pending even though their retained package evidence had already been canonically promoted.

The correction extends the strict `EXPECTED_PASS` evidence table with the exact Batch 3 PASS run/job/artifact/digest/test/SONAME records and updates the canonical count assertion to `10 PASS / 19 pending`. Existing source hashes, KDE dependency metadata and package-evidence checks remain enforced. This is another validator-state synchronization fix and has no package-state effect.

### Validator synchronization sweep

Repository Policy run `34981745762`, job `104423516861`, confirmed that the corrected Tier 1 validator passes at `10 PASS / 19 pending`. The next step, `validate_kde_tier1_packaging_reference.py`, then exposed the same stale pre-Batch-3 assumption: its reference-policy checks still classified `bluez-qt`, `kitemmodels` and `kplotting` as unattempted and expected `7 PASS / 22 pending`.

A proactive audit of the remaining Repository Policy validators found two more copies of that state assumption before rerunning CI:

- `validate_kde_tier1_packaging_tree.py` still knew only the seven pre-Batch-3 PASS nodes;
- `validate_kde_tier1_package_batch1.py` correctly preserved the historical Batch 1 closure at `7 PASS / 22 pending`, but incorrectly required the global current Tier 1 manifest to remain at that historical count.

All three validators were synchronized to the current canonical state without weakening their original contracts. Packaging-reference snapshots remain non-authoritative; packaging-tree evidence remains the retained 58-tree artifact from run `34708030450`, artifact `10301938362`; Batch 1 still verifies its own exact three PASS artifacts and historical `7/22` closure while accepting later independent promotions in the global Tier 1 manifest. Batch 2, Batch 3 and the Attica package validator were audited and did not require equivalent state-count changes.

These policy failures are validator-state synchronization incidents only. They do not create package FAILs, do not alter retained artifacts, and do not change DAG dependency semantics.

### Upstream authority revalidation

KDE upstream was rechecked on 2026-09-15 before Batch 4 preparation:

- KDE Frameworks stable remains `6.30.0`, released 2026-09-09: `https://kde.org/announcements/frameworks/6/6.30.0/`;
- KDE Plasma stable remains `6.7.5`; `6.8` is still Beta as of 2026-09-10: `https://kde.org/announcements/`;
- KDE Gear stable is `26.08.1`, released 2026-09-10: `https://kde.org/announcements/gear/26.08.1/`.

No KDE version transition is required before the next Tier 1 batch.

### Canonical state

Canonical KDE Frameworks Tier 1 state after Batch 3 closure:

**10 PASS / 19 pending / 0 current FAIL / 0 BLOCKED**.

KItemModels, KPlotting and BluezQt were intentionally scope-skipped on the Batch 3 closure/state-only change: package download, build and evidence upload did not run. Retained PASS artifacts were therefore not rebuilt merely for state/evidence/documentation changes.

PR #1 remains Draft. No merge is authorized.

## KDE Frameworks Tier 1 Batch 4 — active remediation

Preparation commit `209f6234cbd94d0b4e02df509b25ae5a6e0922fd` selected KItemViews, KGlobalAccel and KSyntaxHighlighting. Workflow run `34999449194` produced three real attempt outcomes:

- KItemViews `6.30.0-0supralinux1`: PASS, job `104483912528`, artifact `10409267184`, artifact SHA-256 `7e34fa7ede21510cf6829448e7b45a5c2832aaf9ac2719b9cb4125d23b593e55`, tests `2/2 PASS`, Lintian/SONAME/consumer smoke PASS.
- KGlobalAccel `6.30.0-0supralinux1`: real FAIL, job `104483912661`, artifact `10408264332`, artifact SHA-256 `1b7564f211967d1be8b61a96bc2a67389c6164f566e54dad79682d37a33bf6e5`. Clean `sbuild` reached `dh_auto_configure`; ECM `ECMPoQmTools` could not resolve required Qt 6 `LinguistTools` because `qt6-tools-dev` was missing from Build-Depends.
- KSyntaxHighlighting `6.30.0-0supralinux1`: real FAIL, job `104483912313`, artifact `10408154532`, artifact SHA-256 `8601077898fcba1ea5708fbd4e5a1c0d877825f969b8b39999d7d6bf131fbfe4`, with the same independently observed `LinguistTools` provider omission during CMake configure.

The two FAILs are package-attempt FAILs, not BLOCKED and not infrastructure failures. Their evidence is retained permanently.

Ubuntu Resolute `qt6-tools-dev` `6.10.2-1` contains the `Qt6LinguistTools` CMake package requested by ECM. Remediation candidates KGlobalAccel `6.30.0-0supralinux2` and KSyntaxHighlighting `6.30.0-0supralinux2` therefore add `qt6-tools-dev (>= 6.9.0~)` only. KDE source, selected KDE version, source hashes and ABI baselines are unchanged.

Repository Policy on the preparation commit failed only at the Batch 4 validator with `KeyError: 'kitemviews'`. That was a validator assumption about the canonical package DAG, not a package failure. During an open batch, attempt truth is retained in `manifests/kde-tier1-package-campaign-batch4.json`; canonical Tier 1 and downstream promotion remain deferred until the closure commit. The validator is corrected accordingly rather than fabricating a pre-closure DAG PASS.

Canonical global state therefore remains:

**10 PASS / 19 pending / 0 current FAIL / 0 BLOCKED**.

The remediation commit is expected to scope-skip KItemViews and rebuild only KGlobalAccel `-2` and KSyntaxHighlighting `-2`. Batch 4 can close only after real PASS evidence exists for both remediation revisions. PR #1 remains Draft and no merge is authorized.
