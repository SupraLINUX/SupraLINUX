# SupraLINUX status — 2026-09-15 — Tier 1 Batch 6

Status: **Solid retained PASS — KWindowSystem `-4` package/tests PASS; shared consumer-runtime runner remediation pending**

Canonical Tier 1 remains **16 PASS / 13 pending / 0 current FAIL / 0 BLOCKED**.

Batch 6 selects KWindowSystem and Solid because both preserve KDE upstream Linux defaults while fitting the current single-primary-library ABI package lane. Preparation contains no canonical PASS claim and does not promote either node into the canonical DAG.

KWindowSystem retains QML, X11 and Wayland. Solid retains DBus, udev, libmount and optional provider discovery; no upstream Linux default is disabled for packaging convenience.

Official source hashes are pinned in `manifests/kde-tier1-package-campaign-batch6.json`; Debian 6.28 packaging trees remain compatibility references only. Dependency-provider evidence remains run `35012023822`, artifact `10414525047`, and is non-authoritative provider-availability evidence.

Initial run `35034742520` produced two independent real FAIL results:

- KWindowSystem `-1`: job `104601176595`, artifact `10422598658`, SHA-256 `cc38c90312b6e73b2dc338712c391af96664cdd2a342d16f2a163bce67c1be1b`; compile failure because `xcb/xfixes.h` was unavailable. Remediation `-2` adds the `libxcb-xfixes0-dev` Build-Depends present in both retained Debian and Ubuntu references.
- Solid `-1`: job `104601176254`, artifact `10422873422`, SHA-256 `135a63a6855bed690e6bbdbf67dc924b5dbd8bd304f0a78c84920927772e15a0`; build plus `5/5` tests PASS, then Lintian symbols FAIL on `_ZSt19piecewise_construct@Base`. Remediation `-2` applies a hash-locked optional-toolchain symbols delta using Ubuntu's retained `6.4.0`, `!armhf !riscv64` evidence and declares `python3:any` for the transform.

Second run `35038057329` produced one PASS and one independent FAIL:

- Solid `6.30.0-0supralinux2`: **PASS**, job `104611513122`, artifact `10423914110`, SHA-256 `7cc2b15e2fa627e3cd280395e0ac48bd3658c7328d194958b872713738b97fc0`; `5/5` tests, Lintian error gate, SONAME and consumer smoke all PASS.
- KWindowSystem `6.30.0-0supralinux2`: real FAIL, job `104611513304`, artifact `10424431374`, SHA-256 `7b47be8422970fd10c1f67ef450f05bcff9f6086f7f188cd5f91bfcf3c082904`; compilation succeeds, then three NETWM-dependent X11 suites fail under bare Xvfb (`11/14` CTest suites PASS). KDE upstream 6.30 explicitly documents OpenBox as its CI window manager for these tests.

Revision `-3` added only OpenBox/x11-utils as `<!nocheck>` test-fixture inputs and waited for `_NET_SUPPORTING_WM_CHECK` before running the unchanged upstream tests.

Third run `35040999577` proved that the OpenBox fixture resolves the missing-window-manager failure: KWindowSystem `6.30.0-0supralinux3` compiled successfully, OpenBox became ready, the Wayland suite passed and overall CTest advanced to `12/14` suites PASS. The remaining real FAIL is job `104620609155`, artifact `10425162919`, SHA-256 `58481311a264849cbe78c166fcfd2e174d95bc56e04e32be9cab7efed3a2b9dc`.

The two remaining failures were stateful X11 suites sharing the same Xvfb/OpenBox display while `dh_auto_test` ran tests in parallel. Revision `-4` therefore kept Xvfb + OpenBox and every upstream suite but invoked only `dh_auto_test -Skf6 --no-parallel`.

Fourth run `35046462679` proves that the serial test remediation works. Repository Policy `35046462646` passed. KWindowSystem `6.30.0-0supralinux4` completed `sbuild`, passed **14/14 CTest suites**, passed the Lintian error gate and produced the expected binary/source artifacts. The real FAIL is later at `consumer-smoke`: job `104637230834`, artifact `10426857754`, artifact ZIP SHA-256 `ec2af4fb475bfc11f91592d2f18190f39175334b21356b179fc8fd4e60e19ac2`.

The consumer compiled and linked against the built KWindowSystem artifacts, then runtime loading failed because `libxcb-res.so.0` was absent from the hosted runner. The package is not missing that dependency: built `libkf6windowsystem6` correctly declares `libxcb-res0 (>= 1.10)`, and `readelf` records `libxcb-res.so.0` as `NEEDED`.

Classification: `runner/external-runtime-closure-not-materialized`. The old consumer-root extracted the built Batch 6 `.deb` files but did not install their external runtime `Depends`, making consumer results depend accidentally on host preinstalls.

The next remediation is runner-only and does **not** bump package revisions. Before consumer execution, APT will install the exact locally built `.deb` set with `--no-install-recommends`, resolve external `Depends`, run `apt-get check`, and verify exact installed revisions. Consumer compilation still uses the extracted built artifacts and runtime still prioritizes the locally built library through `LD_LIBRARY_PATH`.

Because the shared Batch 6 runner changes, the scope selector intentionally rebuilds both nodes. Solid remains historical PASS at `6.30.0-0supralinux2` but must revalidate under the corrected consumer-runtime contract; KWindowSystem remains `6.30.0-0supralinux4` remediation-pending-build.

Canonical Tier 1 remains unpromoted at **16 PASS / 13 pending / 0 current FAIL / 0 BLOCKED** until the corrected shared lane passes and a separate closure commit promotes the nodes.

PR #1 remains **OPEN + DRAFT**. No merge is authorized.
