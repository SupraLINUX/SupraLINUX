# Project status — 2026-09-11

This document separates facts, decisions, inference, pending work and blockers.

## Facts

- Base platform: Ubuntu 26.04 LTS (`resolute`).
- KDE stable snapshot verified 2026-09-11: Plasma **6.7.5**, Frameworks **6.30.0**, Gear **26.08.1**.
- Plasma 6.7 selects Qt **6.10**; Ubuntu 26.04 exposes `qt6-base-dev` **6.10.2+dfsg-7**. Ubuntu Qt remains a reuse candidate, not certified.
- Hosted `ubuntu-26.04` is non-authoritative preflight.

## Historical Phase 1 build evidence

- `34610526616`: **FAIL**, schroot session creation. Artifact `10268843020`, ZIP SHA-256 `0e17798003bdb563d0fd99a299e8d4b316fd9ffbd2467e0faf70019687c95450`.
- `34612684880`: **FAIL** at `autopkgtest/unshare` testbed construction after source/rootfs/`sbuild-unshare` PASS. Artifact `10268924575`, ZIP SHA-256 `bf7284454e04548936a89bc0576f96bef6ad9d1d141af3c02e817de7f926e0b8`.
- `34615234032`: hosted clean-build **PASS**. Artifact `10269819217`, ZIP SHA-256 `6ac6862bd712e346934472cfc3b6e2c50d49165dbc608216a2ed52eb0623befc`.
  - `.deb`: `3422149c39160b984a1fe3afa8e04286b29050144029a305c5058a7dadc5caf4`
  - `.changes`: `4cc921de00dbd0bf448809f5394bbceb94282fb626a3c39c473d58f81db87839`
  - `.buildinfo`: `7cd6415472b3cf7b1f01e67249b6030210bd4d1011c0b11e622cb7bf49c5c46f`
  - `.dsc`: `8582e9774ab04a9151e4675701bf09a9d815e482c0b7522dd44e4acd364305c4`
  - source tarball: `51e293c475d19d97962d064ac86888bf35da474b4d6de1f5301ed433bd88d668`
  - rootfs: `a7915cb70414103ddb9bcda5228a7ec30c2ce1c8d1a0161e54df7184cdb821eb`
- `34640717571`: later full hosted build proof **PASS**; artifact `10280122077`, ZIP SHA-256 `db0f3688e8964393995607aca59d8adaee378ec250e6b497fbb74d98908f9a49`.

## Repository-side KVM/JIT evolution

- `58ae6a79443a7becdf81082e238b1c9d3c498234`: automated golden-image builder. Policy `34658857826` PASS; hosted scope `34658857824` PASS with `sbuild` skipped.
- `29311b99e71e7550a7c6c1f9878cb16999f7b9a4`: stable source-image storage + source-checkout cleanup. Policy `34659282717` PASS; hosted scope `34659282712` PASS/skipped.
- `e356e4016e6b316dfd977b7a4ecf05aa3fa00655`: exact workflow-run binding + Bash syntax policy. Policy `34659607553` PASS.
- `2b55ffcb683c99c381fef72554aaa606b5c1b370`: real nested-KVM runtime probe. Policy `34659810572` PASS.
- `3fa829078740b68430127c11f5b42cc72c93ef80`: deterministic KVM-only QEMU wrapper. Policy `34660173820` PASS.
- `a11e87e9ac4c5df006dc6ee3dadbce1e39451c5d`: wrapper functional test. Policy `34661595969` PASS; hosted scope `34661595978` PASS with `sbuild` skipped.
- `d8e08bad3774a76b95e8a2f4343ba278894adb96`: Ubuntu ShellCheck gate. Policy `34661759319` PASS using ShellCheck 0.11.0 (`0.11.0-2`).
- `a2cd5a883b9a051b713a9e8817eab9b4a4a6616e`: ShellCheck made a validator/documentation invariant. Policy `34661878334` PASS.

## Signed Ubuntu source-image use-time re-verification

The repository retains Ubuntu's `SHA256SUMS` and `SHA256SUMS.gpg` beside the downloaded cloud image. `scripts/verify-ubuntu-cloud-image-provenance.sh` re-runs `gpgv` immediately before the golden builder inspects/uses the image, derives the expected image SHA from the freshly verified signed metadata, and requires both provenance and actual image bytes to match it.

Transition evidence is retained rather than rewritten:

- `13b844e069b28743218401e65cc4a40f90bdcbb8`, policy `34662331322`: overall **FAIL** because repository invariants still expected identifiers from the prior non-cryptographic verifier. The new signed-source functional test itself PASS.
- `867f11c92577f08b80606a6708c03b65769161c5`, policy `34662446563`: **FAIL** at workflow setup because an invalid `actions/checkout` SHA was introduced during workflow editing. No repository code step executed.
- `52ac79a2a63f8bf0e9c8131fb75420f3229dd2be`, policy `34662544563`: **PASS** after restoring the immutable checkout SHA and aligning invariants. Hosted scope `34662544556` PASS with `sbuild` skipped.
- `f4f89563b9de73dae1ba73d50450c5e481cb706f`, policy `34662703625`: **PASS** with signed-source re-verification made a machine-readable requirement. Hosted scope `34662703619` PASS/skipped.

## Effective GitHub Actions runner provenance

The golden builder verifies GitHub's published SHA-256 for the selected `actions/runner` release and retains its `tag`/asset hash. Because repository JIT configuration does not expose a supported `disableupdate` input, authoritative jobs additionally verify the effective runner binary at execution time.

An upstream audit found that querying `run.sh --version` was not a reliable evidence interface: official `run.sh` delegates through `run-helper.sh`, which can emit wrapper messages around `Runner.Listener`. The correct interface is the runner listener itself, whose upstream command parser handles `--version` and `--commit` before normal run mode.

- `cd736c8ac528cec4aec6e958ab21b0130226c4c5`: initial runtime provenance checker; policy `34662845870` PASS, but later upstream audit identified the `run.sh` evidence ambiguity before any authoritative KVM execution.
- `7ff00724c963352eb60409262ce148ebea87f709`: runtime checker corrected to query `bin/Runner.Listener --version` and `--commit` directly, require a full 40-hex commit SHA and fail closed on version drift from verified golden provenance. Policy `34665058812` **PASS**; hosted scope `34665058807` PASS with `sbuild` skipped.

## Golden-image provenance gate before JIT

The public JIT entrypoint now validates the golden image before any JIT orchestration starts. `scripts/check-golden-image-provenance.sh` requires exactly one valid value for:

- `golden_image_sha256`;
- `source_image_sha256`;
- `source_commit`;
- `source_checkout_removed=yes`;
- `source_image_provenance_verified=yes`.

It recalculates the golden image SHA-256 and fails closed on ambiguity, tampering, missing signed-source proof, missing source cleanup or invalid source commit evidence. `scripts/run-kvm-jit-gate.sh` is the public entrypoint; after this gate succeeds it delegates argument-preservingly to `scripts/run-kvm-jit-gate-core.sh`, which retains the existing serialized/JIT/exact-run-bound orchestration.

Transition and final evidence:

- `70c75d6ffcd1a0be58f883d357abadff77c2935e`, policy `34665229198`: overall **FAIL** only in the old `Validate repository invariants` step because it still looked for JIT-core tokens in the new public wrapper. Bash syntax, ShellCheck, QEMU wrapper, signed-source test, effective-runner test and the new golden-image provenance functional test all **PASS**. Hosted scope `34665229194` PASS.
- `01e26f0d8c172a64983baeef083d0287301404d4`, policy **`34665361682`**: **PASS** across Bash syntax, ShellCheck, KVM-QEMU wrapper, signed Ubuntu source verification, effective Actions runner provenance, golden-image provenance gate, repository invariants and live Qt candidate check. Hosted scope **`34665361725`** PASS with package build and upload explicitly skipped.

## Decisions

- Ubuntu is platform authority; KDE upstream stable is desktop authority. Provider and authority remain separate.
- SupraLINUX remains mutable `.deb`/APT/dpkg.
- Hosted GitHub runners remain non-authoritative preflight.
- Authoritative jobs run in disposable Ubuntu 26.04 KVM/JIT guests.
- Nested KVM is a runtime requirement, not a label assumption.
- `autopkgtest/QEMU` must use the project KVM-only QEMU wrapper; TCG fallback cannot count as authoritative PASS.
- JIT jobs are serialized on the host, reject fork PRs, and bind evidence to one new workflow run ID for the exact PR head SHA.
- Golden source images must pass both fetch-time Ubuntu signature verification and use-time cryptographic re-verification before use.
- A golden image is ineligible for JIT unless its current bytes match provenance and provenance proves signed-source re-verification plus source-checkout cleanup.
- Effective Actions runner version/commit are checked at authoritative runtime through `bin/Runner.Listener`; version drift from the verified golden runner release requires golden refresh.
- Repository Policy requires Bash syntax, ShellCheck, QEMU-wrapper test, signed source-image test, effective-runner test, golden-provenance test, architecture invariants and live Qt verification.
- DAG states remain `PASS`, `FAIL`, `BLOCKED`; `BLOCKED` is not `FAIL`.

## Inference

Repository-side Phase 1 mechanics are now extensively validated. That does not prove actual nested KVM behavior, actual source/golden/test-image hashes on the intended host, runner-group authorization or authoritative package/system-test results; those require real KVM execution.

## Phase 1 gate state

- repository/manifest policy: **PASS** (`34665361682`);
- Bash syntax: **PASS**;
- ShellCheck: **PASS**;
- deterministic QEMU-wrapper functional test: **PASS**;
- signed Ubuntu source-image functional test: **PASS**;
- effective Actions runner provenance functional test: **PASS**;
- golden-image provenance functional test: **PASS**;
- hosted clean `sbuild/unshare`: **PASS**;
- hosted event-delta suppression: **PASS** (`34665361725`);
- JIT/KVM/golden-image repository policy: **PASS**;
- exact workflow-run binding policy: **PASS**;
- real nested-KVM probe implementation/policy: **PASS**;
- use-time signed source-image re-verification implementation/policy: **PASS**;
- actual KVM host preflight: **pending**;
- actual signed Ubuntu source-image fetch/hash on the intended host: **pending**;
- actual golden runner-image build/hash: **pending**;
- actual nested-KVM probe in runner guest: **pending**;
- authoritative runner-contract workflow: **pending**;
- authoritative clean package build: **pending**;
- authoritative KVM-only `autopkgtest/QEMU`: **pending**.

## Current blocker

Further authoritative Phase 1 evidence now requires actual KVM/libvirt execution infrastructure plus host-only GitHub credentials/runner-group configuration. This is an infrastructure dependency, not a package failure. No authoritative PASS or real host/source/golden/test-image hash is claimed before those VMs execute.
