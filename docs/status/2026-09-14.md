# SupraLINUX status — 2026-09-14

Status: **KDE Frameworks Tier 1 hosted package progress: 7/29 PASS**

## Hechos

KDE Frameworks seleccionado permanece en `6.30.0`.

KDE upstream sigue siendo autoridad del stack KDE.

Ubuntu Resolute sigue siendo plataforma y proveedor candidato.

ECM `6.30.0-0supralinux3` permanece PASS.

Tier 1 queda en **7 PASS / 22 pending / 0 FAIL / 0 BLOCKED**.

Los PASS actuales son:

- Attica;
- KCodecs;
- KDBusAddons;
- ThreadWeaver;
- KTextTemplate;
- KArchive;
- KHolidays.

## Batch 2 final

Workflow `34884764702` completó los tres nodos.

Commit probado `e0f4e7c48dcf7541538eb919374a3f4b6c293b75`.

KTextTemplate job `104112549851` PASS.

Artifact `10363863115`.

SHA-256 `7b9d0390ed90c882f4b7ad1993924e722bfb7ad35238c2ced52bbd5809555bc9`.

Tests `10/10` PASS.

KArchive job `104112549742` PASS.

Artifact `10364726750`.

SHA-256 `0fcd8722eddf40995152df200aa576a3ff1234b8135c52e59a66f05dbc8eeb3e`.

Tests `5/5` PASS.

KHolidays job `104112549858` PASS.

Artifact `10364169061`.

SHA-256 `62186f3d6d0c856fed7b055ae917ceec0b6cdf216b37b93e22126bf3ebc8f37a`.

Tests `8/8` PASS.

Todos superaron consumer smoke.

Todos superaron Lintian sin errores.

Todos preservaron `.deb`, `.changes`, `.buildinfo`, `.dsc` y hashes.

Los `.ddeb` también quedaron preservados.

## Batch 1 source+binary Lintian revalidation

Workflow `34892250114` cerró la revalidación del gate Lintian para los tres PASS de Batch 1.

Commit probado `2b0e78b67becc2ece1064816e20b9a2b9d883c47`.

Las revisiones Debian no cambiaron.

El lane preservó los `.ddeb` referenciados por `.changes` y ejecutó Lintian sobre `.dsc` y `.changes`.

KCodecs job `104137548290` PASS, artifact `10367990559`, SHA-256 `434aee54a56b91d6f7791bb02483e61522d287a7ed1920bcb0c2872d177d3ff6`.

KDBusAddons job `104137547996` PASS, artifact `10367472262`, SHA-256 `aac536a623fb4907b89ac73f3241032fa465e43985332eaedfd0a8c699bd76d6`.

ThreadWeaver job `104137547891` PASS, artifact `10367666440`, SHA-256 `acf24b54f72005f3068c6257216d88a47a44efca44302f6b2bed5f6a9d0d6e6c`.

Esta evidencia es hosted y adicional.

No sustituye los PASS históricos originales ni la certificación KVM/JIT autoritativa.

Registro estructurado: `manifests/kde-tier1-batch1-lintian-revalidation.json`.

## Incidente CI corregido

Workflow `34884049556` falló por evidencia incompleta del wrapper.

Los `.changes` referenciaban `.ddeb` no copiados.

Ese incidente no fue un FAIL de paquete.

Commit `e0f4e7c48dcf7541538eb919374a3f4b6c293b75` corrigió la captura.

Workflow `34884764702` validó la corrección.

## KHolidays false PASS histórico

Workflow `34834818877` había sido marcado PASS por el pipeline.

La evidencia `sbuild` contenía un error Lintian real.

Se reclasificó correctamente como FAIL histórico.

Faltaba `python3` en `Build-Depends`.

La revisión `-0supralinux4` usa `python3:any`.

El PASS final confirma la remediación.

## Documentación QCH

Los paquetes Batch 2 `-doc` preservan nombres Debian-family.

Actualmente no contienen payload QCH.

La documentación ya no afirma contenido inexistente.

## Próximo lote

Los candidatos preferidos para Batch 3 son `kitemmodels`, `kconfig` y `bluez-qt`.

Los tres son Tier 1 independientes y no dependen de otros Frameworks KDE.

La selección todavía debe materializarse como packaging SupraLINUX y pasar sus gates reales antes de promover estados.

## Pendiente

Quedan 22 nodos Tier 1 sin intento de paquete.

La prueba KVM/JIT autoritativa sigue pendiente.

La certificación final del proveedor Qt sigue pendiente.

El PR #1 debe permanecer Draft.
