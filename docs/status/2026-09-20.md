# SupraLINUX status — 2026-09-20

## KDE Frameworks 6.30 Tier 1 complete

- Branch: `architecture/bootstrap-v1`.
- Canonical Tier 1: **29 PASS / 0 pending / 0 current FAIL / 0 BLOCKED**.
- Batch 11 final nodes:
  - KUserFeedback `6.30.0-0supralinux5`: run `35489771315`, job `106022610412`, artifact `10597923863`, SHA-256 `0a057d591a198f74cf6eee24d3b1ef4355f2a0d6073d48de8e220ac37f0a38b7`, rootfs `8b5518bea1704ffb9830b4d9a3259e2655ca69efb6dff108b9a9709e66d87e39`, 15/15 tests PASS.
  - Prison `6.30.0-0supralinux1`: run `35488901554`, job `106020266290`, artifact `10597829797`, SHA-256 `f0074c8da29cfda08018fa568fce9cabbf6fc23669416caf20c714c25ff7773f`, rootfs `a1ae6cb653ec18a6dba7142f4ee5acd94210076a9adba23d9d67d03ac03885e7`, 9/9 tests PASS.
- All final ABI, QML import, APT closure, development-contract and external consumer gates are PASS.
- Historical Batch 11 FAIL and INFRA evidence remains retained.
- Global Tier 1 discovery is empty and `multi-surface-optional` is completed.
- PR #1 remains OPEN + DRAFT; no merge is part of Tier 1 promotion.
- This state change does **not** publish anything to the APT `stable` channel.

## Promotion validator incident

Repository Policy run `35491700804`, job `106027643479`, failed only in the historical packaging-reference validator after DAG, Tier 1 and global-discovery validation had already passed. PR CI run `35491700905` passed and scope-skipped all heavy workflows, including Batch 11.

Classification: **INFRA**, `package_attempted=false`, `package_state_effect=none`. Cause: the packaging-reference/tree validators still listed only the previous 27 PASS nodes. The remediation extends those non-authoritative reference validators to KUserFeedback and Prison. Canonical state remains **29 PASS / 0 pending / 0 current FAIL / 0 BLOCKED**.

## Final promotion validation

The promotion-validator remediation is validated on commit `d0c3c66dac717ebc82d6d00632e82e54d43c1dc5`.

- Repository Policy run `35491774160`, job `106027831625`: **PASS** across repository, DAG, Tier 1, global-discovery, packaging-reference/tree and Batch 1–11 validators.
- PR CI run `35491774999`: **PASS**; every heavy reusable workflow was scope-skipped, including Batch 11.
- Package rebuilds: **0**.
- Package-attempt ledger effect: **none**.
- Canonical state remains **29 PASS / 0 pending / 0 current FAIL / 0 BLOCKED**.
- PR #1 remains OPEN + DRAFT and no APT `stable` publication is part of this closure.

## Tier 1 closure validator hardening

- The final global-discovery validator now compares the complete DAG policy object exactly, including `rerun_affected_nodes_after_remediation=true`.
- The Batch 11 validator now requires the recorded promotion-validation INFRA incident and verifies `package_attempted=false` / `package_state_effect=none`.
- This change is validator/documentation-only: canonical Tier 1 remains **29 PASS / 0 pending / 0 current FAIL / 0 BLOCKED** and no package rebuild or APT publication is requested.

### Tier 1 closure hardening validation

- Commit `aa11a9492e5ea2cee9bc6e62703aaa449988494a`.
- Repository Policy run `35492890627`, job `106030747044`: **PASS**.
- PR CI run `35492890731`: **PASS**; every heavy reusable workflow was scope-skipped.
- Rebuilds: **0**; package-attempt/state effect: **none**.
- Canonical Tier 1 remains **29 PASS / 0 pending / 0 current FAIL / 0 BLOCKED**.

## KDE Frameworks Tier 2 discovery

- Revalidation against KDE upstream corrected the Tier 2 inventory from an incomplete **KAuth + KMime** view to the actual **15 Frameworks**.
- Current Tier 2 package state: **1 PASS / 14 pending / 0 current FAIL / 0 BLOCKED**.
- KAuth is retained PASS/downstream-eligible as `6.30.0-0supralinux3`.
- Thirteen additional non-KMime Frameworks are now recorded as **package-lane-pending**: KColorScheme, KCompletion, KContacts, KCrash, KDeclarative, KFileMetaData, KNotifications, KPackage, KPty, KService, KStatusNotifierItem, KUnitConversion and Syndication.
- Their required KDE Framework build predecessors are all retained Tier 1 PASS nodes. KMime is not a predecessor for any of those 13 and therefore does not BLOCK them.
- KDeclarative and KFileMetaData have conditional Framework surfaces that require provider/profile audit before package materialization; several other nodes also expose optional platform/external features.
- KMime remains **compatibility-decision-required** under ADR-0002. Its Tier 1 predecessor KCodecs is PASS, but Frameworks 6.30 changes `KPim6Mime` / `KPim6::Mime` / `libKPim6Mime.so.6` to `KF6Mime` / `KF6::Mime` / `libKF6Mime.so.6`.
- A naïve `6.30.0-0supralinux1` still sorts below Ubuntu's `25.12.3-0ubuntu1`; an epoch alone would not solve the SONAME/CMake transition.
- No newly discovered node has a package version, package attempt, FAIL or BLOCKED result yet.

### Tier 2 inventory correction semantics

The earlier 2-node inventory was a discovery/metadata error, not package evidence. KAuth's source/build/test/ABI hashes and PASS result remain valid. The correction adds 13 pending nodes and changes the current global view to **1 PASS / 14 pending** without changing KAuth's package state or authorizing KMime packaging.

The historical KAuth Batch 1 snapshots `0 PASS / 2 pending` and `1 PASS / 1 pending` are retained only as the campaign state recorded under the incomplete inventory; they are no longer the current Tier 2 scope.

## KAuth Tier 2 Batch 1 initial preparation

- Initial candidate was `kf6-kauth 6.30.0-0supralinux1`; subsequent sections preserve the real attempt history.
- Selected backend remains `POLKITQT6-1` with DBus helper; Fake fallback is forbidden.
- Clean build inputs are pinned retained PASS artifacts for ECM, KCoreAddons and KWindowSystem.
- Debian `kf6-kauth 6.30.0-1` is a technical symbols reference only; its complete `debian.tar.xz` hash is pinned before importing `libkf6authcore6.symbols`.
- The remaining Tier 2 inventory was not part of KAuth Batch 1. KMime remains independently `compatibility-decision-required` under ADR-0002.

## KAuth Tier 2 Batch 1 attempt 1

- Run `35496293561`, job `106039776267`, artifact `10600690671`, SHA-256 `8387279d8cdadd05cf73c2c16177f1d2635a331648638ca3397cef77d04c1ac3`.
- Real FAIL at `source-package`; sbuild did not start.
- Cause: malformed quilt hunk in `helper-install-dir-cache.patch` (7 lines declared, 6 present).
- KAuth advances to `6.30.0-0supralinux2`; only patch structure changes. Source, Polkit/DBus profile and predecessor artifacts remain unchanged.
- KMime remains independently decision-gated.

## KAuth Tier 2 Batch 1 attempt 2

- KAuth `6.30.0-0supralinux2`: source package and clean sbuild reached `dh_makeshlibs`; **6/6 upstream tests PASS**.
- PolkitQt6-1 backend and DBus helper were both built and installed.
- FAIL cause: Debian's 6.30 symbols baseline treats RTTI/vtable for implementation-private `AuthBackend::Private` as mandatory; Resolute does not emit those two entries. Existing missing template-instantiation symbols were already optional.
- Artifact `10601410551`, SHA-256 `4ea7588f1749ce7588cc06fc6bc9dc80350d7fc270bb30688ef173ee6262cfc9`; rootfs `bf90d45c1b750369d482757a4521ea3578a5ef8e10835bf008b258ffad49cbcf`.
- Revision `6.30.0-0supralinux3` marks exactly the two private RTTI/vtable entries `optional=private`; no public symbol is relaxed.

## KAuth attempt 3 preflight validator incident

Repository Policy run `35497248424`, job `106042393365`, failed only because the Tier 2 discovery validator retained the original KAuth revision `-1` while the campaign had advanced to `-3`. The KAuth job had not started its build.

Classification: **INFRA**, no package attempt/state effect. The invariant now compares package identity against the current packaging revision instead of hardcoding a revision.

## KAuth superseded-run scope hardening

KAuth job `106042424661` from PR CI run `35497248610` was superseded while queued and never became a package attempt. The Tier 2 Batch 1 selector now keeps an unattempted prepared/remediation revision runnable until the ledger records `package_attempted=true` for that exact version; after that, normal delta scope resumes.

## KAuth Tier 2 Batch 1 PASS

KAuth `6.30.0-0supralinux3` completed PASS in run `35497461178`, job `106043001431`, artifact `10601382235`, SHA-256 `443a47a67188a52faae6c20b03c2c53203d5a9386dbbb5765af4f69e0cd1744b`, rootfs `15113b108b939e2575758c6696fc34808dfa925fa2dd3cc5e6c13e65e7be4668`.

Final evidence: 6/6 tests PASS, Lintian error gate PASS, PolkitQt6-1 + DBus profile, SONAME `libKF6AuthCore.so.6`, 118 exports, symbols contract PASS, development-contract PASS, APT closure PASS and consumer smoke PASS.

KAuth is promoted/downstream-eligible. The original promotion record was **1 PASS / 1 pending** under the incomplete two-node discovery. After upstream inventory correction, current Tier 2 is **1 PASS / 14 pending / 0 current FAIL / 0 BLOCKED**; 13 non-KMime nodes are package-lane-pending and KMime remains decision-gated by ADR-0002. Repository Policy run `35497460988` had only a scope-test fixture INFRA error (`docs/` missing after reset); the promotion candidate fixed that fixture without rebuilding KAuth.


## Post-KAuth promotion Tier 1 validator incident

Repository Policy run `35498008522`, job `106044531247`, failed after KAuth promotion only in `validate_kde_tier1_package_batch11.py`. All earlier repository, DAG and Tier 1 validators had passed. The historical Batch 11 validator still required the whole DAG to contain exactly 30 nodes (ECM + 29 Tier 1), but KAuth legitimately extended the canonical DAG to 31 nodes.

Classification: **INFRA**, `package_attempted=false`, `package_state_effect=none`. KAuth remains retained PASS/downstream-eligible and Tier 1 remains **29 PASS / 0 pending / 0 current FAIL / 0 BLOCKED**.

Remediation changes the Batch 11 invariant from total DAG cardinality to ownership-scoped validation: ECM must remain PASS and the exact 29 Tier 1 nodes must remain PASS, while higher tiers may extend the DAG.


## Tier 2 complete-inventory correction implementation

The repository now records all 15 upstream Tier 2 Frameworks in the Tier 2 manifest/dependency discovery and validates that exact node set. Required Framework edges were read from upstream `v6.30.0` CMake metadata; source SHA-256 values are pinned from the Frameworks 6.30.0 release source list.

Implementation commits in this correction sequence include:

- `fd206923475b57f3bedb6287ff0efbd6e7c10c0d`: complete Tier 2 inventory;
- `fc11cc45d9d7f6a29eda43a3d3aa4de09b81caec`: dependency discovery expansion;
- `2008f64dac3a834da2c999bdfb951be212699ad6`: reopen all 14 pending Tier 2 nodes;
- `5c48dbecea0a193e0a43693a111e9d532035bbaf`: validator expansion;
- subsequent validator/docs corrections preserve KAuth's selected KWindowSystem edge and historical Batch 1 evidence.

No package build is claimed for the 13 newly added nodes and no APT stable publication is part of this correction.


## Tier 2 campaign automation foundation

- `manifests/kde-frameworks-tier2.json` is now the canonical Tier 2 planning source.
- Every Tier 2 node carries machine-readable planning state for provider audit, package-contract materialization and human blockers.
- `scripts/compile_kde_tier2_campaign.py` deterministically derives `manifests/kde-tier2-campaign-plan.json`; Repository Policy rejects a stale generated plan.
- `scripts/validate_kde_tier2_consistency.py` rejects drift between the canonical manifest, dependency metadata and global discovery views without duplicating the source hashes/dependency tables inside the new validator.
- Current generated execution state is intentionally conservative: KAuth is retained PASS; 13 nodes require provider audit before package-contract materialization; KMime remains human-decision-gated by ADR-0002; the package build queue is empty until those gates are satisfied.
- The generated provider-audit queue is ordered deterministically by conditional-surface count, predecessor count and node id. The first audit batch is KCrash, KNotifications, KStatusNotifierItem, KUnitConversion and Syndication.
- This is process/metadata work only: no package attempt, package-state change, APT publication or `stable` promotion is performed.


## PR CI fan-out scope incident after campaign automation

PR CI router run `35523457436` correctly detected a changed path but classified `.github/workflows/repository-policy.yml` as a possible build/reference/provider input. That caused unnecessary reusable-lane fan-out for commit `2f1e2ec5c232e15a75dc23ed19899946bdde2c2f`.

Classification: **INFRA / CI-scope inefficiency**. The router planning job itself PASSed; this does not alter package state. Heavy package workflows retain their own node-level selectors, so invocation is not equivalent to a real package attempt.

Remediation teaches the semantic router that repository-policy changes, the generated Tier 2 campaign plan, the Tier 2 campaign compiler and planning-only changes inside the canonical Tier 2 manifest are policy/planning deltas. A real Tier 2 source/dependency/build-identity change still requires reusable hosted CI. Scope fixtures now cover both sides of that distinction.


## Atomic commit mode-preservation incident

Repository Policy run `35523551899`, job `106111679059`, failed at `Test PR CI semantic evidence scope` with exit 126 because the atomic Git tree update for commit `2ab7a0798a65a55eada417680c048601c39c77ae` rewrote two executable shell-script entries as mode `100644`.

Classification: **INFRA / tooling**, `package_attempted=false`, `package_state_effect=none`. Shell syntax, ShellCheck and all policy tests before that fixture were PASS. No KDE package was attempted or reclassified by this failure.

Remediation restores `scripts/pr-ci-router-needed.sh` and `scripts/test-pr-ci-router-scope.sh` to `100755`. Future blob/tree/commit updates must preserve the existing Git mode for modified paths rather than assuming `100644`; new directly executed shell scripts must be created as `100755`.


### Campaign automation validation after mode remediation

- Commit `ab4c8a0185947aca3d0f11cc9406efa008b12e8e`.
- Repository Policy run `35523648217`, job `106111942454`: **PASS**.
- The new Tier 2 canonical/derived consistency gate and generated campaign-plan `--check` both passed.
- Package attempts/state effect: **none**.
- The reusable PR fan-out on this commit is not a scope-regression signal because the delta intentionally changed the router script itself.


## Tier 2 provider-audit batch 1 preparation

The generated campaign selected KCrash, KNotifications, KStatusNotifierItem, KUnitConversion and Syndication for the first cheap provider audit.

Upstream Frameworks 6.30.0 profiles were materialized without weakening defaults: KCrash keeps X11; KNotifications keeps DBus, Canberra and Python bindings; KStatusNotifierItem keeps DBus/X11/Python bindings; KUnitConversion keeps Python bindings; Syndication keeps test dependencies. Common Qt/X11/Python provider mappings are inherited from the validated Tier 1 provider registry; Tier 2 adds Canberra.

A dedicated Ubuntu 26.04 provider-audit workflow and evidence contract were added. This stage is explicitly non-authoritative for package build status: no package attempt has occurred and the canonical Tier 2 state remains `1 PASS / 14 pending / 0 current FAIL / 0 BLOCKED`.


## Tier 2 provider-audit batch 1 PASS

Provider audit run `35524034558`, job `106112947247`: **PASS**. Artifact `10609298463`, SHA-256 `8418ca46593df82cdfb6ac306bcebbf97cde44f79db133f704b09e525f01a63d`.

Observed Resolute providers include Qt `6.10.2+dfsg-7`, PySide6/Shiboken6 `6.10.2-6ubuntu1`, `libx11-dev 2:1.8.13-1`, `libcanberra-dev 0.30-18ubuntu3` and `python3-build 1.4.0-1`. Selected CMake/Python/provider probes passed; QML remained optional.

Readiness effect only: KCrash, KNotifications, KStatusNotifierItem, KUnitConversion and Syndication become `package-contract-ready`. Their package state remains `pending`; no package attempt occurred. Canonical Tier 2 remains **1 PASS / 14 pending / 0 current FAIL / 0 BLOCKED**.

Repository Policy run `35524034519`, job `106112947147`, failed only because the consistency validator had not yet learned the new provider-audit transition. Classification: **INFRA / validator state-model transition**, package-attempt/state effect **none**.

The next generated provider-audit batch is KPty, KColorScheme, KCompletion, KContacts and KPackage. The package build queue remains empty.


## Provider-audit evidence rerun scope incident

After provider-audit batch 1 was promoted to readiness in commit `1fa20e13c53c2cc5cf9e45221bf47f00bb555960`, run `35525001576`, job `106115511231`, re-ran the already completed provider audit because the original selector treated every dependency-manifest edit as semantic input. The rerun then failed before provider installation because the closed batch no longer matched the newly generated next-audit queue.

Classification: **INFRA / semantic-scope bug**, `package_attempted=false`, `package_state_effect=none`. The original provider-audit PASS from run `35524034558` remains canonical.

Remediation fingerprints only provider/profile semantics. State/evidence-only promotion changes and runner maintenance after a batch is PASS do not trigger another provider audit. Manual reruns of a PASS batch validate that its nodes remain `package_contract_ready` instead of requiring the historical batch to remain the next queue.


## Tier 2 provider-audit general fan-out scope hardening

The PR router still treated Tier 2 provider-audit workflow/runner changes and dependency-profile edits for unmaterialized Tier 2 nodes as generic build inputs, unnecessarily invoking retained Tier 1/KAuth workflows.

Remediation assigns those inputs to the dedicated Tier 2 provider-audit workflow. Dependency-profile changes remain local while the node has no materialized package contract; once a node is retained PASS or its package contract is materialized, dependency-input changes again require reusable hosted package CI.

This is CI scope only and has no package-state effect.


## Tier 2 discovery validator readiness hardening

Repository Policy run `35525144925`, job `106115883467`, failed only because `validate_kde_tier2.py` still hardcoded all 13 non-KMime pending nodes as `package-lane-pending` after five had legitimately advanced to `package-contract-ready`.

Classification: **INFRA / stale readiness invariant**, `package_attempted=false`, `package_state_effect=none`.

Remediation removes that duplicated readiness constant: active discovery readiness must now equal the canonical planning state in `manifests/kde-frameworks-tier2.json`. Canonical package state remains **1 PASS / 14 pending / 0 current FAIL / 0 BLOCKED**.


## Historical KAuth Batch 1 ownership-scope hardening

Repository Policy run `35525226501`, job `106116092484`, reached the historical KAuth Batch 1 validator after the current Tier 2 validators had already passed, then failed because Batch 1 still required all 13 later non-KMime nodes to remain forever `package-lane-pending`.

Classification: **INFRA / stale historical ownership invariant**, `package_attempted=false`, `package_state_effect=none`.

Remediation keeps Batch 1 authoritative for KAuth history/evidence and verifies that later nodes remain package-state `pending`, while their current readiness is read from the canonical Tier 2 planning state. This allows later provider-audit progress without rewriting KAuth history.


## Tier 2 package-contract reference gate

A first contract draft is prepared for KCrash, KNotifications, KStatusNotifierItem, KUnitConversion and Syndication. Ubuntu/Debian remain technical references only; the source authority is KDE upstream 6.30.0 and the packaging authority is SupraLINUX.

The draft preserves Ubuntu-compatible library/dev/data/QML package names and explicitly retains upstream Python bindings via new SupraLINUX binary splits for KNotifications, KStatusNotifierItem and KUnitConversion. No package state changes yet.

A dedicated Ubuntu 26.04 workflow now captures current Resolute and Debian sid source records, Build-Depends, binary package sets and source-file SHA-256 metadata before any Debian tree is synthesized.


## Tier 2 contract-reference capture PASS

Run `35525532348`, job `106116905607`: **PASS**. Artifact `10609796320`, SHA-256 `0d5d1f961e0189190505545aedca169dc0180ed1d9e189a5eca021267a8385e3`; normalized snapshot SHA-256 `5bfdf1238cedc31ccda2a3f3c93459911709b3e7650eb40c129354f42d0286e9`.

The snapshot pins Ubuntu Resolute `6.24.0-0ubuntu1` and Debian sid `6.30.0-1` technical packaging references for all five contract-ready nodes. KCrash, KNotifications, KStatusNotifierItem and KUnitConversion Debian 6.30 orig hashes match KDE authority. Syndication's Debian orig hash does not match KDE's official `2d44e45...`; the Debian orig is explicitly rejected as a source input while its `debian.tar.xz` may remain a packaging reference.

Repository Policy run `35525532300`, job `106116905418`, failed before contract validation on ShellCheck SC2100 because `STAGE=source-record-capture` was unquoted and parsed as arithmetic-like syntax. Classification: **INFRA / shell lint**, package-attempt/state effect **none**. The stage assignments are quoted in the remediation.

The contract reference is now evidence-complete but package trees remain unmaterialized; canonical Tier 2 package state remains **1 PASS / 14 pending / 0 current FAIL / 0 BLOCKED**.


### Contract-reference revalidation after mismatch policy

Run `35525757861`, job `106117497527`: **PASS**. Artifact `10610520138`, SHA-256 `987ca77884695296ee6d21a841f428f16ed49fb8b103b84a84db8c735ceac9e9`. Normalized snapshot and versions hashes remain unchanged, confirming the source-mismatch policy did not alter the captured packaging facts. Repository Policy run `35525758033`: **PASS**.


## Tier 2 contract evidence-validator hardening

Repository Policy run `35525835689`, job `106117700151`, failed only because the package-contract validator hardcoded the initial reference run/artifact IDs after an equivalent revalidation had been promoted as canonical evidence.

Classification: **INFRA / evidence-identity duplication**, `package_attempted=false`, `package_state_effect=none`.

Remediation validates positive run/job/artifact identities plus the immutable normalized snapshot, versions and result SHA-256 values. Equivalent future revalidation artifacts may replace the transport identity without invalidating the packaging facts.


## Tier 2 deterministic package materialization gate

The first five contract-ready nodes now use a generated packaging-tree model instead of five manually copied Debian trees. Each CI materialization starts from the KDE-authoritative 6.30.0 tarball and overlays only the SHA-256-pinned Debian `debian.tar.xz` reference, followed by an explicit SupraLINUX transformation.

This also prevents a Debian policy choice from silently becoming a KDE feature decision: the Debian 6.30 KNotifications and KStatusNotifierItem rules disable `BUILD_PYTHON_BINDINGS`; the SupraLINUX materializer explicitly restores the upstream-enabled profile and adds dedicated Python binary packages.

The gate produces content-based source/debian-tree hashes and a valid `dpkg-source` package, but it is not a package attempt. Canonical Tier 2 remains **1 PASS / 14 pending / 0 current FAIL / 0 BLOCKED** until clean builds succeed.


## Tier 2 materialization tree-contract harness incident

First materialization matrix run `35527052460` reached the deterministic overlay for all five selected nodes, after validating the exact Debian 6.30 technical-reference hashes and the KDE-authoritative source hash. All five jobs then exited in the common `tree-contract` stage before `dpkg-source -b`.

Classification: **INFRA / common materializer contract harness**, `package_attempted=false`, `package_state_effect=none`. This is not five package FAILs and does not alter the canonical Tier 2 state.

The tree-contract checks are hardened to emit the exact failed invariant plus expected/actual values. Generated `debian/control`, `debian/rules`, materialization metadata and the changelog header are retained in the evidence artifact so a future contract failure is diagnosable without repeating opaque log analysis.


### Tree-contract root cause: absent-field insertion bug

Diagnostic materialization run `35527276119` proved that changelog source/version and the SupraLINUX maintainer were correct while `XSBC-Original-Maintainer` was absent. The first remediation moved that field to the end of the source paragraph.

The follow-up matrix exposed the exact implementation defect before tree validation: generic `set_field()` used `list.append(replacement)` when inserting an absent field even though `replacement` was itself a list. That produced a nested list; in the original mutation order it was accidentally consumed while removing `Uploaders`, and after reordering it correctly surfaced as a Python `TypeError`.

Classification remains **INFRA / materializer transformation bug**, not a package FAIL. The generic primitive now uses `extend()`, so absent Debian control fields are inserted as ordinary lines. The safer mutation order is retained.


### Materialization result-file lifecycle incident

After fixing control-field insertion, run `35527448661` showed that both non-Python and Python-enabled nodes passed all tree-contract checks and successfully executed `dpkg-source -b`. KStatusNotifierItem also passed the explicit Python CMake profile, binary-package and install-module checks.

The jobs then failed only while enriching evidence because `result.json` was read before the `EXIT` trap had created it.

Classification: **INFRA / evidence lifecycle bug**, `package_attempted=false`, `package_state_effect=none`. The result writer is now incremental: pre-exit evidence may create/extend the JSON, and the `EXIT` trap merges final result/stage/exit-code fields without deleting tree/source hashes.


## Tier 2 package materialization 5/5 PASS

Materialization run `35527533481` completed **PASS** for KCrash, KNotifications, KStatusNotifierItem, KUnitConversion and Syndication. Repository Policy run `35527533450` also passed, and the PR router scope-skipped retained Tier 1 and KAuth package lanes.

This is a contract/readiness promotion only. The exact artifacts, artifact SHA-256 values, full-tree hashes, Debian-tree hashes, generated `.dsc` hashes and generated SupraLINUX `.debian.tar.xz` hashes are pinned in `manifests/kde-tier2-package-contracts.json`.

The five nodes advance to `build-ready` / `package_contract=materialized`; their package version candidate is `6.30.0-0supralinux1`. All remain package-state **pending** with `package_attempted=false`. Canonical Tier 2 therefore remains **1 PASS / 14 pending / 0 current FAIL / 0 BLOCKED**.

The generated clean-build queue is now KCrash, KNotifications, KStatusNotifierItem, KUnitConversion and Syndication. No APT publication is performed by this promotion.


## Materialized-contract rerun state-machine incident

After the 5/5 materialization PASS was promoted to `build-ready` in commit `538c65cdcf45ea73d869929754c80e8931c6db13`, materialization run `35527759353` was invoked again. All five jobs failed before overlay validation because the materializer still required top-level contract state `reference-capture-pass`; the promoted state is correctly `materialized`.

Classification: **INFRA / revalidation state-machine and scope bug**, `package_attempted=false`, `package_state_effect=none`. The canonical 5/5 materialization PASS from run `35527533481` remains valid and pinned.

Remediation:
- evidence/promotion-only materialization fields are excluded from the semantic fingerprint;
- the materializer accepts pre-promotion `package-contract-ready` and post-promotion `build-ready` states;
- a materialized PASS revalidation must reproduce the exact pinned full-tree and Debian-tree content SHA-256 values.

Canonical Tier 2 package state remains **1 PASS / 14 pending / 0 current FAIL / 0 BLOCKED**.


## Tier 2 Batch 2 clean-build campaign prepared

The generated Tier 2 build queue is materialized as Batch 2: KCrash, KNotifications, KStatusNotifierItem, KUnitConversion and Syndication. All five remain package-state `pending` until real clean-build results exist.

The workflow builds one Ubuntu Resolute buildd rootfs and shares it across a `fail-fast: false` matrix. Every node consumes the exact retained ECM PASS artifact and exactly one retained Tier 1 PASS predecessor artifact; their artifact IDs and `.deb` SHA-256 values are pinned in the campaign.

Attempt semantics are enforced in the runner: pre-`sbuild` failures are INFRA; `package_attempted=true` is set immediately before `sbuild`; post-boundary failures are real package FAILs. PASS requires tests, exact binary contract, Lintian, ABI, APT closure and external consumer gates, plus Python/QML smoke where applicable.

The Batch 2 workflow is dedicated and excluded from the legacy PR-router fan-out. This preparation does not publish any package.


## Batch 2 pre-execution harness correction

Before any Batch 2 package job reached `sbuild`, review of the newly generated runner found two harness-only defects: an invalid `@{CHROOT_TARBALL}` expansion and a leftover diagnostic Bash substitution. The shared rootfs is now linked into sbuild's standard `~/.cache/sbuild/resolute-amd64.tar` location and the normal unshare invocation is used.

Classification: **INFRA / pre-execution runner correction**, `package_attempted=false`, `package_state_effect=none`. No Batch 2 package result is inferred from the superseded run.


## Batch 2 ShellCheck preflight incident

Repository Policy run `35530903286`, job `106131185986`, stopped at ShellCheck before any Batch 2 package job reached `sbuild`. The generic runner loaded `EXPECTED_BINARIES` into a Bash array even though the exact binary-set validation is performed directly by the embedded Python contract check, so ShellCheck reported SC2034.

Classification: **INFRA / static-lint cleanup**, `package_attempted=false`, `package_state_effect=none`. The redundant Bash variable is removed; the exact binary-set gate itself is unchanged.


## Tier 2 Batch 2 first clean-build campaign — shared provider/tooling root cause

Run `35530953084` created shared Resolute rootfs artifact `10611208232`, SHA-256 `eda2af33ba8d468bd2a13c461d838a89ed80e6f3c48434a27686b548d2f87b2b`.

KCrash (job `106131492912`), KNotifications (`106131492935`), KStatusNotifierItem (`106131492962`) and Syndication (`106131492929`) all crossed the clean-`sbuild` boundary and then converged at `install-deps` on the same unsatisfied `debhelper-compat (= 14)` Build-Depends inherited from the Debian 6.30 technical-reference packaging. No node reached compilation.

Classification: **shared provider/packaging-tooling INFRA**, not four node-owned package FAILs. These are preserved as real executions because `sbuild` started, but `package_state_effect=none`. Canonical FAIL is reserved for a cause owned by the node.

KUnitConversion (job `106131492939`) did not reach `sbuild`. Its retained KI18n artifact matched every expected binary except the campaign transcription for `libkf6i18n-data`: expected `3df2300d5aca640c8968859367ac3cd606a31e87e875ca207a71eeda6d03f45a`, artifact-verified `3df2300d5aca640c8968859367ac3cd606a31a87e875ca207a71eeda6d03f45a`. Classification: **pre-attempt manifest-pin INFRA**; the pin is corrected.

Provider diagnosis: Ubuntu Resolute supplies the debhelper 13 compatibility surface, while the pinned Debian 6.30 packaging reference asks for compat 14. Debian remains a technical reference, not authority. SupraLINUX therefore applies an explicit deterministic packaging-tooling adaptation `14 -> 13` in `debian/control`, with `kde_feature_effect=none`, and requires a fresh clean build to validate it.

The previous materialization PASS from run `35527533481` is retained as historical evidence but marked superseded for build consumption. KCrash, KNotifications, KStatusNotifierItem, KUnitConversion and Syndication temporarily return to `package-contract-ready`; the generated build queue is empty until replacement trees are materialized and pinned.

Canonical state remains **Tier 1: 29 PASS / 0 pending / 0 current FAIL / 0 BLOCKED; Tier 2: 1 PASS / 14 pending / 0 current FAIL / 0 BLOCKED**. No APT publication or stable promotion is authorized by this remediation.


### Tier 2 discovery-validator ownership correction

Repository Policy run `35534384596` reached the Tier 2 discovery validator after every earlier policy gate passed. The validator incorrectly treated the temporary rematerialization state as if no package contract had ever existed, requiring the five retained package-version candidates to become null, and also required the discovery snapshot to mirror current package readiness.

Classification: **historical/ownership validator regression**, `package_attempted=false`, `package_state_effect=none`. The discovery validator now owns discovery facts only. Package-contract identity/version and current readiness are validated from the canonical Tier 2/package-contract manifests; the discovery snapshot is no longer allowed to freeze later packaging-state transitions.


### Tier 2 canonical/derived consistency ownership correction

Repository Policy run `35534475926` passed the updated Tier 2 discovery validator and then stopped at `validate_kde_tier2_consistency.py` for the same stale ownership assumption: it required the discovery snapshot's readiness field to mirror the canonical package-planning readiness during temporary rematerialization.

Classification: **historical/derived-view validator regression**, `package_attempted=false`, `package_state_effect=none`. Dependency edges, source metadata, provider-audit state and the global PASS/pending/FAIL/BLOCKED snapshot remain cross-checked against canonical data; the discovery readiness field is now validated only as discovery vocabulary and cannot freeze later package-contract transitions.


## Tier 2 provider-adapted rematerialization 5/5 PASS

Materialization run `35534384634` on commit `f2927e550d113369a33011d7737136a0bb91f272` completed **PASS** for KCrash, KNotifications, KStatusNotifierItem, KUnitConversion and Syndication. Each tree passed the explicit `debhelper-compat (= 13)` provider-level contract and produced a valid source package from the KDE-authoritative orig tarball plus the pinned Debian packaging reference.

New artifacts:
- KCrash: `10611834586`, SHA-256 `96d776e28bd474511a3318e6ef2a56f5e399cbed23b1578593cd54fafff7c577`;
- KNotifications: `10612293440`, SHA-256 `bf14082d63bd8967dd30e6db8797fa48264ee8a2d2d13c88ec47e76e13d43573`;
- KStatusNotifierItem: `10611879452`, SHA-256 `37873531a138bb5ed0b22219d06b69ae7cf6ec0db057a85251d87865e18c0da9`;
- KUnitConversion: `10612467659`, SHA-256 `0afef762199e72b0dfcd5a6fc35b2791f689726684d6be08ee653a428e6cf868`;
- Syndication: `10612213475`, SHA-256 `77c8d4e621fac3bd0cc01505f06a1874d73582cc34776181ba0298293d7ffef2`.

The five nodes return to `build-ready` / `package_contract=materialized` and remain package-state `pending`. Batch 2 advances to `remediation-pending-build`. The previous materialization and first shared-provider incident remain historical evidence.

Runner semantics are also corrected: a post-`sbuild` execution failure is emitted as `ATTEMPT_FAILURE` pending root-cause classification; canonical `FAIL` requires a node-owned cause. No APT publication is performed.


## Tier 2 Batch 2 remediation attempt — two shared integration causes

Run `35534595946` completed with five job failures after a clean shared Resolute rootfs was created (artifact `10612413376`, SHA-256 `6f983685852266dbe08e4401cb9a1ec2cf1804b403ab97b916db43732f8d3a8c`). All five package jobs crossed the `sbuild` boundary, so they are retained as real attempts, but root-cause classification shows **no node-owned FAIL**.

KCrash and Syndication compiled and produced binary packages before Lintian rejected `unreleased-changes`. The cause is the SupraLINUX materializer's generated changelog distribution `UNRELEASED`; remediation changes it to the actual base target, `resolute`.

KNotifications, KStatusNotifierItem and KUnitConversion failed in Shiboken ApiExtractor. The common warning states that Clang's built-in include directory could not be located through `LLVM_INSTALL_DIR`, `CLANG_INSTALL_DIR` or `llvm-config`; all three then failed parsing at `/usr/include/c++/15/cstddef -> stddef.h`. Resolute's default `llvm-dev` provider is added to these Python-binding Build-Depends so `/usr/bin/llvm-config` and the matching LLVM 21 development surface are available.

Classification for both clusters: **shared integration/provider INFRA**, `package_state_effect=none`. Canonical Tier 2 remains **1 PASS / 14 pending / 0 current FAIL / 0 BLOCKED**.

The materialization from run `35534384634` is preserved as historical PASS evidence but superseded for build consumption. The five nodes return temporarily to `package-contract-ready`, the generated Batch 2 build queue is empty, and corrected package trees must be rematerialized before another clean-build campaign. No APT publication is authorized by this remediation.
