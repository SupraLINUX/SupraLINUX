# Project status — 2026-09-12

This document records work performed on 2026-09-12. Earlier Phase 1 and Qt history remains in `docs/status/2026-09-11.md`.

## Facts

- Selected desktop remains Plasma 6.7.5, Frameworks 6.30.0 and Gear 26.08.1.
- Ubuntu Qt 6.10.2 provider baseline preflight is PASS; final Qt provider certification remains pending.
- The real KDE package DAG starts from Extra CMake Modules 6.30.0.
- KDE upstream is the source/version authority for ECM; Ubuntu's ECM package is a technical packaging reference only.
- ECM remains **FAIL**. Tier 1 has not been attempted and is not mislabeled as FAIL.

## ECM attempt 1 — FAIL

Commit `99b95b1c0897ed20af6c8add7aa1620ef1795019` attempted package `6.30.0-0supralinux1`.

- Repository Policy run `34689672651`: **PASS**.
- ECM workflow run `34689672632`: **FAIL**.
- evidence artifact ID: `10296512341`;
- artifact digest: `sha256:d47ac7361106ceb5f04729c1a3dcb4103b30255684e5b3f53b833edd6d4f6558`;
- failure stage: `dh_auto_test`.

Root cause: the package-preflight profile intentionally used `BUILD_TESTING=OFF`, which removed Ninja's `test` target, while debhelper still invoked `dh_auto_test`/`ninja test`. This was a packaging-integration FAIL, not an ECM source/toolchain failure. The reusable sbuild rootfs path/format was also corrected after this attempt.

## ECM attempt 2 — FAIL

Commit `801b99792dc60e7c14cb78d1846f9b8c9a476de8` attempted package `6.30.0-0supralinux2`.

- ECM workflow run: `34690027788`;
- job: `103543538213`;
- result: **FAIL**;
- evidence artifact ID: `10296517706`;
- artifact digest: `sha256:89ef0003fd8282965a4c253bcd0150b8f040fe134ca0385d1365cd8c31808239`;
- `result.json` final stage: `consumer-smoke`.

What actually happened:

- KDE upstream source SHA-256 verification: PASS;
- Debian source-package creation: PASS;
- clean `sbuild --chroot-mode=unshare` binary build: PASS (`Status: successful`);
- the `dh_auto_test` issue from attempt 1 was resolved;
- Lintian emitted two package errors: `no-copyright-file` and `python3-script-but-no-python3-dep`, plus the source warning `no-debian-copyright-in-source`;
- sbuild reported the Lintian policy violation but still returned a successful binary-build status, so the preflight previously did not stop there;
- the subsequent Qt-integrated consumer CMake smoke failed in `ECMQueryQt.cmake` because no Qt 6 `qtpaths` executable was available in the consumer environment.

This is therefore still a root-node **FAIL**, with two independently actionable packaging/preflight defects. It is not a Qt provider rejection: the consumer environment simply had not installed/exposed the already selected Ubuntu Qt 6.10 development tooling.

## Remediation candidate — 6.30.0-0supralinux3

The next attempt uses a new Debian revision rather than rebuilding changed packaging under `-0supralinux2`.

Changes:

- add `debian/copyright`, documenting the upstream BSD-3-Clause/BSD-2-Clause/MIT aggregate license set while retaining KDE's per-file SPDX declarations as authority;
- add `python3:any` to the binary package runtime dependencies for installed ECM Python helper scripts;
- install `lintian` on the preflight host and run `lintian --fail-on error` as an explicit fatal post-build gate;
- install Ubuntu `qt6-base-dev`, expose `qtpaths6` on `PATH`, and retain its version as consumer-smoke evidence;
- capture `.deb`, `.changes`, `.buildinfo`, control/filelist and hashes immediately after successful build, before Lintian and consumer gates, so a later FAIL still has inspectable build artifacts.

The manifest deliberately remains **FAIL** and marks `6.30.0-0supralinux3` as `pending-validation` until CI actually attempts it.

## Test-scope boundary

This package-preflight profile does not claim execution of the full ECM upstream test suite. Its purpose is verified KDE source + clean Debian package build + fatal packaging-policy check + Qt-integrated downstream consumer smoke. The upstream test suite remains a separate quality gate before production/stable certification.

## Next gate

Run the ECM workflow for `6.30.0-0supralinux3`. Only an observed PASS with retained package hashes, Lintian PASS and consumer-smoke PASS may promote ECM to PASS and make its artifact eligible for Frameworks Tier 1. Otherwise retain the new attempt as FAIL, diagnose the root cause, and leave downstream nodes unattempted/BLOCKED by dependency semantics.
