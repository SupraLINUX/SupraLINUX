# SupraLINUX status — 2026-09-21

Canonical KDE Frameworks Tier 2 starts the day at **6 PASS / 9 pending / 0 current FAIL / 0 BLOCKED** after Batch 2 closed 5/5 PASS. KDE Frameworks 6.30.0 remains the latest official stable release checked against KDE upstream on 2026-09-21.

## Provider audit batch 2

Selected nodes: KPty, KColorScheme, KCompletion, KContacts and KPackage.

Run `35597828609`, job `106326647774`, stopped before APT/provider probing because the dependency manifest lacked the Qt/external/`selected_linux_profile` materialization for all five nodes. This is **INFRA / definition-validation**, `provider_checks_started=false`, `package_state_effect=none`; it is not evidence that a Resolute provider is unavailable.

The missing profiles are now derived from KDE upstream v6.30.0 CMake. KPty selects Resolute `libutempter-dev`; KColorScheme selects the Qt 6.10 GuiPrivate branch; KCompletion preserves the native designer plugin; KContacts preserves QML; KPackage preserves Linux DBus while refusing to substitute its optional KDE DocTools with an older distro Framework.

The provider-audit CMake probe is now generated from the active batch's Qt requirements instead of a fixed first-batch list. The same `pending-ci` batch must rerun and reach actual provider checks before any of these nodes can become package-contract-ready.

No APT publication or stable promotion is performed.


### Provider audit batch 2 — provider installation succeeded, runner assertion corrected

Run `35598315301`, job `106328176979`, progressed beyond definition validation and successfully resolved/installed the generated provider set. Evidence in the job summary includes `libutempter-dev 1.2.1-4build1` and the selected Qt 6.10 DBus/QML/Quick/Designer/private-Gui surfaces.

Failure occurred afterwards because the generic audit runner still unconditionally queried `libshiboken6-dev`/`libpyside6-dev`, packages required by the previous Python-binding audit but not by this batch. Classification: **INFRA / stale optional probe**, `package_state_effect=none`.

The runner now executes PySide/Shiboken alignment and Python-build probes only when those providers are in the active mandatory package set. The active five-node audit remains `pending-ci` and must rerun to completion before promotion.


## Provider audit batch 2 PASS and promotion

Run `35605754366`, job `106352296102`, artifact `10641708858` (SHA-256 `d77732753fd7801b4e3525391636a61a11d5b4f40e8c43d461da08dbc5be7bb8`) completed PASS for KPty, KColorScheme, KCompletion, KContacts and KPackage.

The audit verified Ubuntu 26.04, Qt upstream/provider `6.10.2`, all mandatory Qt CMake components for the five-node profile, and KPty's selected UTEMPTER integration. These five nodes are now `package-contract-ready`; package state remains pending.

Generated campaign state: 6 retained PASS, 5 package-contract-ready, 3 still requiring provider audit (KService, KDeclarative, KFileMetadata), and KMime awaiting ADR-0002. No package materialization/build PASS is inferred from the provider audit, and no APT publication occurred.


## Parallel Tier 2 lanes activated

Two independent lanes are now opened in parallel:

1. **Package-contract batch 2:** KColorScheme, KCompletion, KContacts, KPackage and KPty enter technical-reference capture. Materialization is fail-closed until that capture is PASS.
2. **Provider-audit batch 3:** KService, KDeclarative and KFileMetaData enter provider/profile validation.

Upstream dependency corrections were applied before execution: KDeclarative selects Linux KGlobalAccel/KWidgetsAddons; KFileMetaData treats KCoreAddons/KCodecs as required and selects available KArchive/KConfig integrations. Canonical package state remains **6 PASS / 9 pending / 0 current FAIL / 0 BLOCKED**.

No APT publication is performed.
