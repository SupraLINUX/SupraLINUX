# SupraLINUX status — 2026-09-18

## Current canonical facts

- KDE Frameworks campaign series in the repository: `6.30.0`.
- Canonical Tier 1 package state after Batch 9 promotion: **25 PASS / 4 pending / 0 current FAIL / 0 BLOCKED**.
- KGuiAddons `6.30.0-0supralinux2` is PASS and downstream-eligible.
- Historical Batch 8 attempts 1–4 remain retained as FAIL evidence; attempt 5 is the retained PASS.
- Only retained PASS artifacts may feed downstream nodes. BLOCKED remains distinct from FAIL.
- PR #1 remains Draft; no merge is authorized by this promotion.

## Batch 8 retained PASS

KGuiAddons final evidence:

- head/checkout: `de47462c5b6b21bc2c485f5dd2c39035a32f7f45`;
- workflow run: `35185562846`;
- job: `105086774400`;
- artifact: `10482007092`;
- artifact ZIP SHA-256: `71d32ecb50f6617ba198325a552e68e995c20c9050c7ae21f6a69d5b680184ca`;
- rootfs SHA-256: `c68681aacfd32976c6e0bf471ec1928179fd80e402faf2293706e53f88ad25c6`;
- tests: `9/9 PASS`;
- Lintian error gate: PASS;
- Python import: PASS;
- exact APT closure/check: PASS;
- consumer smoke: PASS;
- SONAME: `libKF6GuiAddons.so.6`.

The package source remains KDE upstream `kguiaddons-6.30.0.tar.xz`, SHA-256 `e98864228d1c5e23f3428025eb9928697374fdc3eddebb3a9dec570de028a62d`.

## Dependency model

KGuiAddons remains a Tier 1 Framework with no KDE Framework build dependency. Its canonical DAG dependency is only `extra-cmake-modules`.

KCoreAddons is retained solely for the public `KImageCache` development/consumer surface: `libkf6guiaddons-dev` exposes the KCoreAddons development dependency, but KCoreAddons is not added to KGuiAddons `Build-Depends` and is not a DAG build predecessor.

## Next development lane

Four Tier 1 nodes remain pending:

- QML/multisurface: `kirigami`, `kquickcharts`;
- multi-surface/optional: `kuserfeedback`, `prison`.

Batch 9 is **3/3 canonically promoted**. KI18n `6.30.0-0supralinux1` and Sonnet `6.30.0-0supralinux3` retain their cycle-4 PASS results. KConfig `6.30.0-0supralinux4` retains the final PASS after adding `qt6-declarative-dev` to close the exported `Qt6Qml >= 6.9.0` development contract. Canonical state is **25 PASS / 4 pending / 0 current FAIL / 0 BLOCKED**.

## CI validator boundary after Batch 8 promotion

The first Repository Policy run on the promotion commit reached the packaging-tree policy validator after Tier1, DAG, global-discovery and packaging-reference validation had already passed. That validator still encoded the previous **21 PASS / 8 pending** live count and omitted KGuiAddons from its set of later real package PASS nodes.

Classification: **repository-policy validator state mismatch**. This is infrastructure-only and has no package-state effect. KGuiAddons remains the retained Batch 8 PASS from run `35185562846`; no package rebuild or revision change is required.

The validator is updated to recognize KGuiAddons and the canonical **22 PASS / 7 pending** state while preserving the historical packaging-tree capture evidence from run `34708030450`.


## Batch 9 multi-ABI preparation

Batch 9 attempt 1 ran KConfig, KI18n and Sonnet at `6.30.0-0supralinux1`. All three depend only on retained ECM `6.30.0-0supralinux3` at the Framework DAG level and were independently attempted. Source diagnostic run `35138333645` remains **DIAG_PASS only** and cannot promote package state.

The Batch 9 runner validates fresh Resolute `sbuild`, non-zero upstream CTest PASS, Lintian error gating, exact package splits/Multi-Arch metadata, all retained ABI symbol baselines, SONAMEs, QML imports, exact local-package APT closure and C++ consumer smoke. KConfig carries only the technical compiler-install-dir layout patch; KI18n keeps upstream tests enabled with the validated locale fixture; Sonnet excludes Debian `cross.patch` and keeps Widgets/QML/Designer/backends enabled.

The append-only Batch 9 ledger now records attempts 1 and 2 for all three nodes. KI18n is PASS in both real attempts; KConfig and Sonnet are FAIL in both, with attempt 2 isolating the remaining issue to the direct `python3:any` build prerequisite. No peer is BLOCKED.


## Batch 9 Repository Policy shellcheck incident

Repository Policy run `35348129610`, job `105609486001`, failed at the repository-wide ShellCheck gate with `SC2100` on the diagnostic string assignments `STAGE=qml-package-contract` and `STAGE=qml-import-smoke`. Classification: **infrastructure-only; package_state_effect=none**. The package matrix in PR CI is a separate real attempt and continues independently. The remediation quotes those two stage-label values only; build/test/ABI/package/runtime behavior is unchanged.


## Batch 9 attempt 1 results

PR CI run `35348130023` on commit `958d9990f0c5c6e85ade45faf8b7fa1a62c0c3af` produced three real independent package results:

- KConfig `6.30.0-0supralinux1`: **FAIL**, job `105609530611`, artifact `10549115856`, ZIP SHA-256 `6579cc838436e1390c950da9037997ae4646fb4fb40d5e69290377c16f385b6c`, rootfs `79fe82c1adfffb5050106b07157c423ed7c9c7c0a4a2608ace84899d5ae1184e`, tests `90/90 PASS`. The package build completed; Lintian rejected the newly generated `KStandardActions::staticMetaObject` symbol at the Debian revision. KDE Frameworks 6.29.0 introduced `StandardAction` as `Q_ENUM_NS`, so the reviewed ABI minimum is `6.29.0`.
- KI18n `6.30.0-0supralinux1`: **PASS**, job `105609530707`, artifact `10548710619`, ZIP SHA-256 `d213147477242b08a1dac78611b30eacb60f9bd727abb1313d5a2c8bc7c24cf4`, rootfs `f3849a2ceb003aeb1de899fe3b56dec59b3b29da6589f79da9b7dc3685c52b4d`, tests `17/17 PASS`; Lintian, ABI, QML imports, APT closure and consumer smoke PASS. This PASS is retained but not yet canonically promoted.
- Sonnet `6.30.0-0supralinux1`: **FAIL**, job `105609530666`, artifact `10549100349`, ZIP SHA-256 `d01d279dc2645efd9ecc01506e17c0f99aecadda761bc12236af7ae1341a3fc6`, rootfs `300d410c9e584cd0bc8dee75578db2ac940363e20440a289c2a31142b64d7d09`, tests `8/8 PASS`. The package build completed; Lintian rejected the new `Settings::defaultSkipRunTogether()` symbol at the Debian revision. KDE Frameworks 6.30.0 records the upstream function-name typo fix, so the reviewed ABI minimum is `6.30.0`.

KConfig and Sonnet remediation uses deterministic hash-guarded symbols deltas before `dh_makeshlibs`; KI18n remains at its retained PASS revision. Canonical Tier 1 remains **22 PASS / 7 pending / 0 current FAIL / 0 BLOCKED** until a separate promotion commit.


## Batch 9 attempt 2 results

Repository Policy run `35354088344` passed all policy steps, including ShellCheck and the Batch 9 validator. PR CI run `35354088696` then produced:

- KConfig `6.30.0-0supralinux2`: **FAIL**, job `105629086800`, artifact `10551565818`, SHA-256 `44095dd2f16b7101c0f1dbe65fb561d79361b28a8b5f255151760e64a4ea09a3`, rootfs `a3ef32e90b7f8c734110becf63a327503855e3c5d495fb340668f944cb24b30c`, tests `90/90 PASS`. The reviewed symbols delta passed; Lintian requires a declared `python3:any` build prerequisite.
- KI18n `6.30.0-0supralinux1`: **PASS**, job `105629086722`, artifact `10550338682`, SHA-256 `08271a40a392b0d947b05413c54c913230261d26bff1ed12d91d37a85400a3bd`, rootfs `2e45730952a04dc7b8ed5fe96c6e479427697a5d6fc0c840cc8817fe81c24974`.
- Sonnet `6.30.0-0supralinux2`: **FAIL**, job `105629086871`, artifact `10551700066`, SHA-256 `3d995b55c0e106554957bd7cfc2a1f63df19fc3c7b95a59be9fa0df51a9f6c23`, rootfs `bca53d5e3e2f5e536987ad3d8885ba44d814db9a2bd80b7ed07b006bc394b5f7`, tests `8/8 PASS`. The reviewed symbols delta passed; Lintian reports the same missing direct Python build prerequisite.

Attempt 3 changes only KConfig/Sonnet package metadata to `6.30.0-0supralinux3` by adding `python3:any` to `Build-Depends`. KI18n remains retained PASS and should scope-skip.


## Batch 9 validation cycle 3

PR CI run `35355395436` on `97e4feb51df541501b027ba80c4b86907d8763c8` produced two infrastructure false negatives after otherwise successful package builds:

- KConfig `6.30.0-0supralinux3`: job `105633436677`, artifact `10551433966`, SHA-256 `eed45f10d52f3190fcf099ceb63c4138f1c346d191d6aa85429b1beafd829b95`, rootfs `cd684394244cb651b6d36171bf140e8dfae4905d92b5f4da2d87d89b66754629`, tests `90/90 PASS`, Lintian PASS. ConfigCore emitted 640 exports; retained baseline total 648 contains 6 optional exports and 8 non-optional exports inapplicable to amd64, leaving 634 required.
- Sonnet `6.30.0-0supralinux3`: job `105633436582`, artifact `10551832586`, SHA-256 `f9352e5f5c3cd527cc32887896b1c6c4441e1291ee103204de470e3378a77218`, rootfs `9e79ae0631e0c4b7a59f3fb82af976eaaf9c388c01b57edfebd30001f37d38af`, tests `8/8 PASS`, Lintian PASS. SonnetCore emitted 255 exports; retained baseline total 263 contains 9 optional exports, leaving 254 required.

Classification for both: **INFRA / package_state_effect=none**. The shared runner now validates total, optional, architecture-inapplicable and required amd64 counts independently from the retained baseline and compares generated exports only against the required amd64 floor. This runner-only remediation does not bump package revisions and forces a three-node revalidation.


## Batch 9 validation cycle 4

Repository Policy run `35358919925` is PASS. PR CI run `35358920602` produced two retained package PASS results and one real KConfig package FAIL:

- KI18n `6.30.0-0supralinux1`: PASS, job `105645094825`, artifact `10553916882`, SHA-256 `2a0d66f2760c49ba1128b8b51c741173a72e6f3ab51f3eb17c3584896d30a678`, rootfs `c3a542cadce65b491d0997f7fa61cfabc2b44188a2021c151858349766fd148d`.
- Sonnet `6.30.0-0supralinux3`: PASS, job `105645094787`, artifact `10554216487`, SHA-256 `ae5a33cf8699b96b7b7b8c1a83bc4d37a484878029818c1885b4f4f44f15b431`, rootfs `38f9fcd9dd6cb379bd5ba1fbfb5c93f57b49db71c166615415ff663366fc54bb`.
- KConfig `6.30.0-0supralinux3`: FAIL at `consumer-smoke`, job `105645094816`, artifact `10553307816`, SHA-256 `7bc04355d35d9bd86033ffaceb89a2addbb79e9e5d49ef14f59be58f2cfde312`, rootfs `3c863b17d63bf75492b41bcb774d6d40616ffe646ded3d0c50fb51562c17df57`. All earlier gates passed, including `90/90 PASS`, Lintian, architecture-aware ABI, exact APT closure and QML import.

KConfig revision `6.30.0-0supralinux4` adds `qt6-declarative-dev (>= 6.9.0~)` to `libkf6config-dev` Depends because the exported `KF6ConfigConfig.cmake` requires `Qt6Qml >= 6.9.0`. This is a consumer-development closure fix. KI18n and Sonnet remain retained PASS and must scope-skip on the next KConfig-only package change.


## Batch 9 validation cycle 5 — campaign complete

Repository Policy run `35360530531`, job `105650391908`, passed. PR CI run `35360530830` rebuilt only KConfig; KI18n and Sonnet were intentionally scope-skipped and retained their previous PASS evidence.

KConfig `6.30.0-0supralinux4`: PASS, job `105650448776`, artifact `10554715051`, SHA-256 `bd36a8288c1c36fa2ae1a83d685a816cc1077fc576b59a23951972ddcbdd835e`, rootfs `cf14256f216dd3ec9a67a7bca3bd46e8624391ffe40b2c08567dc1fe90a4b9e3`, tests `90/90 PASS`, Lintian/ABI/APT/QML/consumer gates PASS.

Batch 9 is therefore **3/3 PASS retained** and ready for separate canonical promotion. Historical package FAILs and infrastructure-only cycle-3 incidents remain preserved. No canonical node is promoted by this evidence-recording step itself.


## Batch 9 closure validator case-sensitivity incident

Repository Policy run `35365460369`, job `105666708183`, failed only at the Batch 9 validator because its documentation-token check was case-sensitive for the strings `validation cycle 4/5`. All previous policy validators passed and the PR CI Batch 9 jobs scope-skipped all three retained PASS nodes.

Classification: **infrastructure only / package_state_effect=none**. The validator is corrected to compare documentation tokens case-insensitively. Batch 9 remains 3/3 retained PASS and canonical Tier 1 remains 22/7 pending the separate promotion commit.


## Batch 9 canonical promotion

The dedicated promotion follows the validated 3/3 PASS campaign. Final precheck Repository Policy run `35365719747`, job `105667583358`, passed. PR CI scope-skipped KConfig `105667619494`, KI18n `105667619517` and Sonnet `105667619509`; no package rebuild occurred during promotion.

Canonical state is now **25 PASS / 4 pending / 0 current FAIL / 0 BLOCKED**. KConfig, KI18n and Sonnet are downstream-eligible canonical DAG nodes. Remaining pending nodes are `kirigami`, `kquickcharts`, `kuserfeedback` and `prison`. PR #1 remains Draft and unmerged.


## Batch 10 QML/multisurface preparation

Batch 10 is implementation-ready for Kirigami and KQuickCharts while canonical Tier 1 remains **25 PASS / 4 pending / 0 current FAIL / 0 BLOCKED**. Kirigami is directly runnable. KQuickCharts has no KDE Framework build predecessor, but its full QML/runtime package validation requires a retained SupraLINUX Kirigami PASS because `qml6-module-org-kde-quickcharts` imports/depends on the Kirigami QML surface. This is package-validation ordering, not a new upstream Framework edge.

The lane preserves upstream 6.30 defaults, keeps tests enabled, uses the retained ECM/package-tree/binary-contract evidence, validates architecture-aware ABI floors, enumerates all packaged `qmldir` modules, runs QML import scanning and clean C++ consumers, and never treats a predecessor-caused non-attempt as FAIL. The first Batch 10 attempt ledger is intentionally empty.


### Batch 10 preparation ShellCheck incident

Repository Policy run `35388377180`, job `105740650985`, failed only in the Batch 10 scope-test harness with ShellCheck `SC2251`. This occurred before Batch 10 validators and has **package_state_effect=none**. The negative scope assertions are rewritten without bare `!` under `set -e`; the selector contract remains `0=rebuild / 1=skip`. A new package campaign run is required after the corrected Policy passes; results from the pre-fix commit are not used for promotion.


### Batch 10 preparation validator incident

Repository Policy run `35388572182`, job `105741309585`, passed ShellCheck, Batch 10 scope testing and global discovery, then hit a false-positive Batch 10 validator assumption: it demanded a literal KQuickCharts path instead of the selector's generic `packages/kde/${NODE}/*` contract and looked for package-validation metadata in the wrong file class. Classification: **infrastructure / package_state_effect=none**. The validator is corrected and a fresh package campaign is forced by the shared-selector clarification.


### Batch 10 runner behavior-guard incident

After Repository Policy run `35388829511` passed, PR CI `35388829761` selected Kirigami but job `105742185938` stopped before source/rootfs build because the runner recursively matched forbidden-token documentation under `debian/`. Artifact `10564903426`, SHA-256 `07e820838fb1192118c207cbbb919ad0246987ae973781c72f67acda013a3534`. Classification: **infrastructure / package_state_effect=none**; no Kirigami FAIL or QuickCharts attempt is created. The guard now inspects executable rules/test commands only.

## Batch 10 attempt 1 and Kirigami ABI remediation

Repository Policy run `35389029819` passed. PR CI run `35389030840` produced the first real Batch 10 package result: Kirigami `6.30.0-0supralinux1` is **FAIL** at Lintian after a successful build and `44/44 PASS` upstream tests. Evidence: job `105742841430`, artifact `10565625878`, ZIP SHA-256 `58b5bc72f81cdd26ea368cf810d1e1727fbdfc521203fe889bca005aa2f9aca8`, rootfs SHA-256 `445ce98267d53ec58a98bed3ee74343f04f2465bdccf258efa95e709551ff7b1`.

KQuickCharts was not attempted. Job `105745672521` correctly recorded **BLOCKED** by Kirigami; artifact `10565780756`, SHA-256 `118afc6b702b4ec9fee050898fcbc381f5beaf2131013b619973c85e81f27ef8`. This is not a KQuickCharts FAIL.

Kirigami moves to `6.30.0-0supralinux2`. Five retained symbol baselines receive only deterministic, hash-verified `optional=templinst` overlays for the 38 Qt/libstdc++ template-instantiation exports. The exact nine QMetaType/QMetaSequence symbols are documented by KDE bug 519452 as parallel-build-dependent. No public ABI minimum is invented and no active `.symbols` file is committed. `python3:any` is declared because `debian/rules` executes the delta helper.

Canonical Tier 1 remains **25 PASS / 4 pending / 0 current FAIL / 0 BLOCKED** until a separate successful promotion.
