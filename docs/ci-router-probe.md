# CI router documentation-only probe

Temporary validation file for the PR event-delta router.

Probe iteration 3: final documentation-only routing check after repository and Qt validators were aligned with the centralized PR router.

Expected result: Repository Policy runs normally; PR CI router executes only its planning job and skips all 18 reusable package/reference/provider lanes.

This file has no architectural authority and will be removed after the live routing check.
